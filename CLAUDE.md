# 모아랩 업무 워크스페이스 (moalab-work)

모아킷/모아랩 **내부 전용** 업무 관리 웹앱. 외부 판매용 아님.
사용자는 원장 1명 + 강사 4~6명. **전원 스마트폰 접속이 기본**, PC는 원장이 주로 쓴다.

## 이 프로젝트가 해결하려는 것

노션으로 관리하면서 안 되던 세 가지:

1. 강사가 폰에서 보면 표가 가로로 길어서 자기 할 일을 못 찾는다
   → **강사 홈은 "내 할 일" 부터, 원장 홈은 "전체 현황·일정" 부터** (`isAdmin` 으로 순서만 바꾼다)
   → 그리고 **`내 할 일` 말고는 전부 접어둔다.** `order-*` 는 순서만 바꿀 뿐
     **길이를 1px 도 안 줄인다** — 그게 홈이 3~4화면이던 진짜 이유였다
   · 본문을 `flex flex-col` 로 두고 섹션마다 `order-*` 만 역할에 따라 다르게 준다.
     JSX 를 두 벌 쓰지 않으니 내용이 갈라질 일이 없다.
   · 한동안 이게 **구현이 안 된 상태**였다 — 강사도 원장과 같은 순서라
     `내 할 일` 이 1706px(3화면째)에 있었다. 지금은 283px(첫 화면)이다.
2. 웹앱을 수정해도 이전 검증 체크가 남아 뭘 다시 봐야 할지 모른다 → **검증 라운드**
3. 원가·작품 사진·수업계획안이 흩어져 있다
   → **프로그램 페이지 한 장에 전부** (아래 "프로그램 페이지" 참고)

## 가장 중요한 전제

> **관리 대상 웹앱은 계속 늘어난다.**
> 앱을 추가할 때 **코드를 절대 건드리지 않는다.** 원장이 화면에서 버튼으로 추가한다.

앱 관련 화면·쿼리는 전부 데이터 기반이다. 앱이 21개든 200개든
`useAppsOverview` 의 쿼리 수는 그대로다. 새 앱을 추가할 때 수정할 파일은 **없다**.

---

## 기술 스택

| | |
|---|---|
| 프레임워크 | Next.js 14 (App Router) + TypeScript |
| 스타일 | Tailwind CSS · **다크 대시보드** (브랜드 `#F26522`, 보조 `#2AD1C8`) |
| 폰트 | 에스코어드림 (S-Core Dream, OFL) — jsDelivr CDN |
| 로고 | 모아킷 (M 심볼 + moakit 워드마크), 브랜드 teal `#06BDBD` |
| DB / Storage | Supabase |
| 로그인 | 자체 PIN 4자리 (Supabase Auth 안 씀) |
| AI | Claude API (`@anthropic-ai/sdk`) — **말로 업무 넣기 한 곳에만**. 키·모델은 원장이 화면에서 고른다 |
| 배포 | Vercel |

---

## 시작하기

```bash
# 1) 의존성
npm install

# 2) 환경변수
cp .env.example .env.local
#    Supabase 대시보드 > Project Settings > API 에서 세 값을 복사해 채운다

# 3) DB 스키마
#    Supabase 대시보드 > SQL Editor 에 supabase/schema.sql 을 통째로 붙여넣고 실행
#    (여러 번 실행해도 안전 / Storage 버킷 6개와 초기 멤버 5명까지 같이 만들어짐)
#    ※ 예전에 돌린 DB 라면 그 뒤에 생긴 버킷이 없다 → supabase/storage.sql 만 다시 붙여넣기

# 4) ★ Supabase 설정 > API > Exposed schemas 에 moalab 추가 후 Save
#    이걸 빼먹으면 앱이 데이터를 하나도 못 읽는다

# 5) 개발 서버
npm run dev        # http://localhost:3000
```

**초기 PIN** — 로그인한 뒤 `관리 > 멤버`에서 반드시 바꿀 것.

| 이름 | PIN | 역할 |
|---|---|---|
| 강양희 | 0000 | admin |
| 이서은 | 1111 | teacher |
| 주은서 | 2222 | teacher |
| 강지연 | 3333 | teacher |
| 윤창진 | 4444 | teacher |

### Vercel 배포

프로젝트 환경변수에 `.env.local` 과 같은 세 개를 넣는다.
`SUPABASE_SERVICE_ROLE_KEY` 는 **절대 `NEXT_PUBLIC_` 을 붙이지 않는다** (서버에서만 씀).
기한 알림을 쓰려면 `CRON_SECRET` 도 넣는다 — 없으면 `/api/cron/due` 가 401 로 막힌다.

---

## 화면 구조

메뉴 순서 = **일하는 순서**다.
공지로 알리고 → 계획을 세우고 → 검증하고 → 모의수업으로 돌려보고 → 강사를 키운다.
원가·갤러리·일정은 그 흐름에 딸린 **자료**라서 아래 묶음으로 내렸다.

| 경로 | 메뉴 | 내용 |
|---|---|---|
| `/home` | 홈 | 대시보드 — 인사 배너 · **일정·마감**(달력+타임라인) · 전체 현황(통계+팀) · 공지 · 내 할 일<br>(PC 는 오른쪽에 팀 활동 · 주간 활동 · 프로그램 구성) |
| `/notice` | 공지사항 | 공지 + **첨부(사진·한글·PDF)** + **읽음 표시** + 검색·안읽은것만. 쓰기는 원장만 |
| `/task` | 업무 | **누가 · 무엇을 · 언제까지.** 담당자 1명 + 기한 + 3단 상태. 강사는 내 것만, 원장은 사람별 |
| `/apps` | 프로그램계획 | 목록(리스트·보드·갤러리) → `/apps/[id]` **프로그램 페이지** |
| `/verify` | 프로그램검증 | 검증만 모아 보기 — 내가 볼 것 / 내 답변 대기 / 전체 |
| `/mock` | 모의수업 | 날짜·발표자만 잡고, 끝나면 "좋았던 점 / 고칠 점" 두 칸 |
| `/training` | 강사양성 | 과정 × 강사 이수 상태 (미이수·진행 중·이수) |
| `/roles` | 역할분장 | **부서 › 중분류 › 소분류**, 소분류마다 주담당 1명 + 부담당 |
| `/cost` | 원가 | 원가표 목록 → `/cost/[sheetId]` 계산서 |
| `/revenue` | 월별 수익배분 | 달별 프로그램 수금·직접비·성과몫 → 사람별 월 예상액 합산 |
| `/expense` | 지출결의서 | 쓴 돈 + 영수증 → 달별 문서로 인쇄 (`/print/expense`) |
| `/gallery` | 갤러리 | 앨범/사진 → `/gallery/[albumId]` |
| `/schedule` | 일정 | 월간·주간 달력 |
| `/admin` | 관리 | **원장만.** 멤버, 앱 추가, 전체 현황, 활동 로그 |

- **PC**: 왼쪽 사이드바에 전부 세로로 (업무 8개 → 구분선 → 자료 4개).
- **폰**: 하단 탭은 4개(홈·공지·프로그램·검증)만 두고 나머지는 **더보기** 시트에 넣는다.
  탭을 9개 늘어놓으면 375px 에서 글씨가 뭉개진다.
  · **업무도 더보기에 있다.** 5개로 늘리면 라벨이 뭉개져서, 대신 **홈에서 자기 업무가 다 보이게** 한다.
    (원장이 자주 들어가느라 불편하면 `검증`과 자리를 바꾼다 — `PHONE_TABS` 한 줄이다)
- 메뉴는 `src/components/BottomNav.tsx` 의 `WORK_NAV` / `RESOURCE_NAV` 두 배열이 전부다.
- **아직 내용이 없는 메뉴는 `quiet: true` 로 아래 묶음(`아직 안 쓰는 것`)에 내린다.**
  지금은 모의수업·강사양성·갤러리 셋이다 (데이터 0건). **지우지는 않는다** —
  지우면 첫 한 건을 넣을 방법이 없어진다. 쓰기 시작하면 이 표시만 떼면 제자리로 올라온다.
  섞어두면 매일 쓰는 업무·지출이 빈 화면들 사이에 파묻힌다.

---

---

## 전용 스키마 `moalab` — 이걸 먼저 알아야 한다

이 Supabase 프로젝트는 **다른 앱과 같이 쓴다.** `public` 스키마에는 이미
`members` · `apps` · `schedules` · `photos` 같은 남의 테이블이 있다.
그래서 모아랩 테이블은 **전부 `moalab` 스키마 안**에 있다.

- 브라우저·서버 클라이언트 모두 `db: { schema: 'moalab' }` 로 만든다
  (`src/lib/supabase.ts`, `src/lib/supabaseAdmin.ts`)
  → 앱 코드에서는 그냥 `supabase.from('apps')` 라고 쓰면 `moalab.apps` 로 간다.
- SQL 로 직접 볼 때만 스키마를 붙인다: `select * from moalab.apps;`
- Storage 버킷도 같은 이유로 `moalab-` 접두어를 붙였다
  (`moalab-comment-files`, `moalab-cost-photos`, `moalab-gallery`, `moalab-plans`,
  `moalab-notices`, `moalab-receipts`).
  접두어 없이 `gallery` 를 쓰면 남의 버킷을 공개로 덮어쓸 위험이 있다.
- **Supabase 설정 > API > Exposed schemas 에 `moalab` 이 없으면 아무것도 안 된다.**
  이 경우 `friendlyError()` 가 그 사실을 한글로 알려준다.

> 새 테이블을 추가할 때도 반드시 `moalab.` 을 붙이고, `schema.sql` 의
> RLS 목록(`internal_all` 루프)에 테이블 이름을 넣어줘야 한다.
>
> **⚠️ 딱 두 표는 예외다 — `members`(PIN)와 `app_secrets`(API 키).**
> 이 둘은 `internal_all` 배열에 **절대 넣지 않는다.** 넣는 순간 브라우저의 anon 키로
> PIN 과 API 키가 통째로 읽힌다. 대신 RLS 만 켜고 정책은 만들지 않은 뒤
> `revoke all ... from anon, authenticated` + `grant all ... to service_role` 로 잠근다.
> 접근은 서버 라우트(`/api/login`, `/api/members`, `/api/settings/ai`, `/api/task/parse`)에서만.

### ⚠️ `service_role` 권한을 빼먹지 말 것

`public` 스키마와 달리 **커스텀 스키마는 Supabase 기본 권한이 안 붙는다.**
`anon`/`authenticated` 만 주고 `service_role` 을 빠뜨리면
`/api/login` 이 `permission denied for schema moalab` 으로 죽는다
(로그인 화면에 이름은 뜨는데 PIN 을 넣으면 실패 → 원인 찾기 어렵다).

```sql
grant usage on schema moalab to anon, authenticated, service_role;
grant all on all tables in schema moalab to anon, authenticated, service_role;
grant all on moalab.members to service_role;   -- members 는 anon revoke 후에도 남겨야 함
```

검증 환경을 만들 때도 **슈퍼유저가 아니라 진짜 `service_role`** 로 테스트해야
이 구멍이 잡힌다.

---

## 프로그램 페이지 — "따로국밥" 을 없앤 곳

노션에서 가장 답답했던 게 **웹앱 · 검증 · 수업계획안 · 원가 · 샘플이미지가 다 딴 데** 있던 것이다.
그래서 `/apps/[id]` 한 페이지에 전부 올렸다. 위에서 아래로 읽으면 그게 곧 일하는 순서다.

| 섹션 | 내용 | 데이터 |
|---|---|---|
| 속성 블록 | 상태·라운드·제작자·검증자·마감·학년·링크 | `apps`, `app_reviewers` |
| ✅ 검증 | 캡처 + 지적 + 답변, 검증 완료 표시, 지난 라운드 | `rounds`, `findings`, `finding_files`, `finding_replies`, `round_signoffs` |
| 📄 강의계획서 | **한글 양식 그대로 채우는 칸** — 그대로 A4 로 인쇄된다 | `lesson_plans`, `lesson_plan_items` |
| 📎 문서 첨부 | **갈래(계획안·교육안·양식·기타) + 판(버전)** — 최신만 크게, 지난 판은 접힘 | `plan_files` |
| 💰 원가 | 1인당·총원가·마진 요약 (고칠 땐 `/cost/[id]`) | `cost_sheets`, `cost_items` |
| 💰 수익배분 | 월별 역할 성과몫 + 남은 이익 1/N 계산 링크 | `revenue_share_plans`, `revenue_share_months` |
| 🖼️ 샘플 이미지 | 제안서용 예시 작품 | `app_samples` |
| 📸 수업 사진 | 실제 수업 기록 (앨범 링크) | `albums`, `photos` |
| 💬 댓글 | 스크린샷 첨부 | `comments`, `comment_files` |

- 상단 목차 칩은 스크롤에 따라 따라간다 (`IntersectionObserver`).
- **샘플 이미지 vs 수업 사진**: 샘플은 "이 수업 하면 이런 게 나온다"(제안서용),
  수업 사진은 "언제 어느 학교에서 뭘 했다"(기록용). 목적이 달라 저장소를 나눴다.

### 목록은 주제별 트리부터

`/apps` 는 **주제별(트리) · 리스트 · 보드(상태별) · 갤러리** 네 가지 보기를 제공한다
(선택은 localStorage 에 기억).

**기본은 주제별이고, 주제는 접힌 상태로 시작한다.** 폰에서 21개를 한 줄로 늘어놓으면
5000px 가까이 되어 맨 아래까지 스크롤해야 다 볼 수 있었다.
접어두면 프로그램이 몇 개든 **첫 화면 안에서 전체 구조가 보인다.**

- 주제는 **`moalab.topics` 테이블**이고 `apps.topic_id` 가 가리킨다.
  **프로그램마다 타이핑하지 않는다** — `프로그램계획 > 주제 관리`(원장만)에서 한 번 만들면
  앱 폼에서는 드롭다운으로 고르기만 한다. 폼 안에서 `+ 새 주제` 로 즉석 추가도 된다.
  · 이름을 고치면 그 주제를 쓰는 프로그램 **전부에 한 번에** 반영된다 (FK 라서 update 한 줄)
  · 순서도 원장이 정한다 (`sort_order`) — 트리 순서가 그 순서를 따른다
  · 주제를 지워도 프로그램은 안 지워지고 **주제 없음**으로 돌아간다 (`on delete set null`)
  · 이름은 `unique` — 같은 주제가 두 개 생기지 않는다
  · **`주제로 옮기기`** — 프로그램을 체크해서 한꺼번에 옮긴다. 21개를 하나씩
    `[수정 → 주제 → 저장]` 21번 하는 건 말이 안 된다. 지금 주제별로 묶은 **한 줄 목록**을
    보여주고 묶음 전체 선택도 되며, 저장은 `update ... in (...)` **요청 하나**다.
    '주제 없음' 묶음이 맨 위에 온다 — 그게 채워야 할 것이다.
  · 처음엔 자유 입력(`apps.topic` text)으로 만들었는데 "매번 만들어야 하냐" 는 지적을 받고
    관리 테이블로 바꿨다. `schema.sql` 이 기존 자유 입력 값을 `topics` 로 옮겨준다(공백 정리 포함).
    `apps.topic` 컬럼은 기록용으로만 남겨뒀다.
- **목록 기본값은 `할 일`** — 검증 완료된 건 빼고 보여준다. 다 끝난 것이 목록을 채우고 있으면
  지금 봐야 할 게 뭔지 안 보인다. `전체` 칩을 누르면 완료된 것까지 다 나온다.
  · 완료된 프로그램은 그냥 사라지는 게 아니라 목록 맨 아래 **`검증 완료 보관함`**(접힘)에
    따로 모인다 — "어디 갔지?" 가 안 되게. 검색어가 있으면 보관함도 저절로 펼쳐진다.
    보드 보기의 빈 '검증 완료' 칸도 기본 화면에서는 안 그린다 (보관함이 대신한다).
  · 상태 칩은 `할 일 / 전체 / 내 담당 / 수정 필요 / 다시확인 / 진행 중 / 검증 완료` 일곱 개이고
    한 줄 가로 스크롤이다 (여러 줄로 깔면 폰에서 목록이 밀린다).
  · **기본값 `todo` 는 '검색·필터가 걸렸다' 로 세지 않는다.** 세버리면 첫 화면부터
    주제가 전부 펼쳐져서 접어둔 이유가 사라진다 (실제로 그렇게 만들었다가 812px → 2030px 이 됐다).
- 비어 있으면 **주제 없음** 으로 묶이고 늘 맨 아래다.
- 접힌 머리글에 **그 주제의 프로그램 이름을 한 줄로 싣는다** (개수·수정필요·완료는 제목 옆 작은 글씨).
  주제 16개에 프로그램 33개라 주제마다 2개꼴인데, 예전엔 `2개` 라고만 적혀 있어서
  **두 화면을 넘겨도 프로그램 이름이 하나도 안 보였다.** 이름을 실으면 펼치지 않고도 찾는다
  (머리글 87px → 65px, 목록 전체 1702px → 1567px). 펼치면 같은 이름이 카드로 나오니 그때는 안 그린다.
- **검색·필터가 걸리면 저절로 펼친다.** 접힌 채로 0건처럼 보이면 안 된다.
카드마다 **검증·계획안·강의계획서·원가·샘플·사진** 6개가 채워졌는지 배지로 보인다.
강의계획서는 **목표를 써서 저장했으면** 완성으로 친다 (`lesson_plans.goal`).
"빠진 것" 필터로 *원가 없는 프로그램만* 같은 걸 바로 뽑을 수 있다.
관련 계산은 `useAppsOverview` 의 `Completeness` 에 모여 있다.

---

## 핵심 도메인 규칙

### 검증 = 캡처 + 지적 + 답변

> **고정 5항목 O/X 표는 걷어냈다.** 표에 O/X 만 치면 정작 *어디가 왜* 안 되는지가
> 안 남아서 아무도 다시 못 찾았다. 검증은 **화면을 캡처해서 뭐가 이상한지 적는 것**이다.

지적 한 건(`moalab.findings`)이 이렇게 닫힌다.

1. **캡처하고 지적 남기기** — 이상한 화면을 캡처(여러 장, `finding_files`) + 무엇이 어떻게
   이상한지 글(필수). 캡처는 권장이지 필수가 아니다 — "AI 응답이 안 온다" 처럼
   화면에 안 잡히는 문제도 있는데 캡처를 강제하면 아예 못 적는다.
2. **답변** (`finding_replies`) — 나든 다른 사람이든 **누구나** 셋 중 하나 + **글(필수)**
   · `fixed` **수정완료** → 무엇을 어떻게 고쳤는지
   · `wontfix` **수정불가** → 왜 이대로 두는지
   · `recheck` **다시확인** → 무엇을 더 봐야 하는지
   답변을 올리면 지적의 `status` 가 그 값으로 옮겨간다. 답변은 **쌓인다**(스레드).
   글 없이 상태만 바꾸는 건 막는다 — 다음 사람이 못 알아본다.
3. **확인완료로 닫기** — 지적을 낸 사람(또는 원장)이 닫는다 → `closed`.
   · **검증자가 '검증 완료' 를 누르면 그 라운드의 `fixed` 는 한꺼번에 닫힌다.**
     '검증 완료' 가 곧 "고친 걸 내가 확인했다" 는 뜻이라, 지적을 하나씩 또 닫게 하면
     같은 일을 두 번 시키는 것이다. 예전엔 `fixed` 가 하나라도 남으면
     *"아직 안 닫힌 지적이 있어요. 먼저 확인완료로 닫아주세요"* 라며 버튼이 **아예 안 눌렸다.**
   · 아직 손봐야 하는 `open`(지적됨)·`recheck`(다시확인)는 여전히 검증 완료를 막는다.
     막을 땐 몇 건인지 세어서 **버튼 안에 미리** 적어준다 — 눌러보고 에러로 알게 하지 않는다.
   · 갈래는 `src/lib/types.ts` 의 `blocksSignoff` / `isConfirmableFinding` 두 함수가 전부다.

### 검증자는 누구나 — 자기 이름을 눌러 참여한다

원장이 미리 배정해두는 방식이 아니다. **본인이 스스로 점검하고 자기 이름을 체크한다.**

- **앱을 만들 때 검증자를 안 골라도 저장된다.** 예전엔 "최소 1명" 을 요구해서
  누가 볼지 정하지도 않은 프로그램을 등록조차 못 했다. 이제 비워두고 저장하면 되고,
  각자 프로그램 화면에서 자기 이름을 눌러 들어온다. (원장이 미리 정해두고 싶을 때만 고른다)
- 검증자 0명이면 상태는 `pending` 이다 — 지적이 0건이어도 `done` 이 되지 않는다.
- 검증 섹션 아래 패널에 **멤버 전원**이 칩으로 보인다.
- **내 이름은 내가** 눌러서 참여/빼기 (`app_reviewers` upsert/delete).
  남의 이름은 **원장만** 누를 수 있다 (`canToggle = isMe || isAdmin`).
- 참여를 빼면 그 사람의 `round_signoffs` 도 같이 지운다 — 안 그러면 정족수가 어긋난다.
- 칩 색: 회색 = 미참여 / 브랜드 = 참여 중 / 초록 = 검증 완료.

`FindingStatus` 5개 중 **`open` · `fixed` · `recheck` 는 아직 살아있는 것**으로 센다
(`isOpenFinding`). `wontfix` · `closed` 만 닫힌 것이다.
`fixed` 를 살아있는 쪽에 둔 이유: 제작자가 고쳤다고만 한 상태이고
**검증자가 확인해야** 진짜로 닫힌다. 그 확인이 바로 **검증 완료** 버튼이다
(누르는 순간 그 라운드의 `fixed` 가 `closed` 로 넘어간다).

관련 코드는 `src/components/Findings.tsx` 한 파일에 모여 있다.
답 안 한 지적은 `/verify` 의 **내 답변 대기** 칸에 모인다.

- 캡처는 `moalab-comment-files` 버킷에 올린다 (댓글 사진과 같은 버킷 — 성격이 같다).
- (구) `checks` / `check_files` 테이블은 **지난 기록을 지우지 않으려고 남겨만 뒀다.**
  화면에서는 안 쓴다. 새 코드는 `findings` 를 본다.

### 강의계획서 — 한글 양식을 화면으로 옮겼다

원장이 쓰던 한글 양식(`강의계획서.hwp`)을 그대로 화면 칸으로 만들고, 그대로 A4 로 인쇄한다.
한글을 열어 타이핑하고 다시 올리는 왕복이 없어진다.

- **양식 한 장 = 프로그램 하나.** 그래서 `lesson_plans.app_id` 가 그대로 기본키다.
- 칸 순서 = 인쇄물 순서다. `PlanForm`(작성)과 `PlanSheet`(인쇄)의 칸 이름·순서를 **같이** 고쳐야 한다.
  · 제목줄(프로그램명 + 분류 배지) · 과정/강의내용 머리 · 목표 · 도입 · 전개 · 마무리 · 운영사항(교구·기타사항)
  · 표는 3칸이다. 보통 줄은 `[과정 | 내용(2칸 병합)]`, 운영사항만 `[운영사항(2줄 병합) | 교구·기타 | 내용]`
- **도입·마무리는 프로그램이 달라도 늘 같은 문구**라 DB 기본값으로 미리 채워둔다.
  그대로 두면 그대로 인쇄되고, 고치면 고친 대로 나간다.
- **전개 칸은 글 + 사진 한 장이 한 줄**(`lesson_plan_items`). `slot` 으로 갈래를 나눈다.
  · `step` [AI 웹앱활동] 1·2·3 — 인쇄에서 **가로 3칸**으로 늘어선다
  · `order` 만드는 순서 · `result` 결과 이미지 — 인쇄에서 **웹앱활동처럼 가로 한 줄**
    (묶음마다 한 줄, 최대 3칸, 넘치면 다음 줄). 세로로 쌓았더니 사진 몇 장에
    한 장이 꽉 차서 사진만 작아졌다. **사진이 1장뿐인 묶음은 반 폭으로 가운데** 놓는다.
- **맨 아래 로고는 박아넣지 않고 올리는 칸**(`logo_url`)이다. 기관마다 다르고 언제든 바뀐다.
- 저장은 **저장 버튼**이지만, **사진과 활동 줄은 고르는 즉시 저장**한다.
  사진을 올려놓고 저장을 안 눌러 업로드가 날아가는 게 더 나쁘다.
- 인쇄에서 이 장은 **늘 새 쪽에서 시작한다**(`print-page-break`). 앞에 검증 기록이 붙어도
  이 한 장만 떼어 쓸 수 있어야 한다.
- **이 장은 늘 A4 단면 한 장이다.** `PlanSheet` 가 장 전체를 인쇄 영역 크기(186×268mm)로
  고정해 두고, 사진 칸 높이(`--ph`)를 "안 넘치는 가장 큰 값"으로 잰다(이진 탐색).
  사진을 몇 장을 올리든 **사진이 줄어들 뿐 두 쪽으로 안 넘어가고**, 내용이 적으면
  같은 탐색이 사진을 키워 아래까지 채운다 (예전 `fill` 옵션이 하던 일까지 겸해서 옵션을 없앴다).
  글이 병적으로 길면 사진을 최소로 줄여도 넘치는데, 그때는 넘친 부분이 잘린 채
  **화면 미리보기에 그대로 보인다** — 화면에서는 같은 장을 폭에 맞게 축소해 보여주므로
  (transform, 인쇄에서는 `.print-a4-sheet` 가 원래 크기로 되돌림) 화면 배치가 곧 인쇄 배치다.
- **한글 파일(.hwpx)로 받을 수 있다** — 학교·기관에 hwp 로 내야 할 때.
  · `.hwp`(클래식)는 한컴 독점 바이너리(CFB)라 브라우저에서 만들 방법이 없다.
    `.hwpx` 는 **국가표준 KS X 6101(OWPML)** 이고 ZIP + XML 이라 만들 수 있다.
    한글 2010 SE+ 부터 열리고 2014+ 부터 안정 편집된다. 받은 뒤 한글에서
    `다른 이름으로 저장 → hwp` 하면 .hwp 가 된다.
  · **새 라이브러리를 안 넣었다** — ZIP 은 이미 쓰는 jszip, XML 은 `src/lib/hwpx.ts` 가 직접 짓는다.
  · 배치는 인쇄물·원본 양식과 같다 — 표 8줄, 사진은 가로 한 줄(최대 3칸).
    **1장뿐인 묶음은 안쪽 표를 만들지 않고 반 폭으로 가운데** 놓는다
    (빈 칸을 옆에 붙이는 식으로 하면 왼쪽으로 쏠린다).
  · **한글이 파일을 통째로 거부하는 조건이 까다롭다.** 아래를 어기면 "열 수 없습니다" 가 뜬다:
    `mimetype` 은 `application/hwp+zip` + zip 첫 항목 + 무압축(STORE) /
    속성에 네임스페이스 접두어를 붙이지 않는다(`paraPrIDRef`, `hp:paraPrIDRef` 아님) /
    첫 문단에 `hp:secPr`(용지 설정) / 표 칸마다 `cellAddr`·`cellSpan`·`cellSz`·`cellMargin` +
    **병합돼 가려진 칸은 아예 안 그린다**.
  · **사진은 넣기 전에 JPEG/PNG 로 바꾼다.** 이 앱은 올린 사진을 전부 **WebP** 로
    저장하는데(`image.ts`) **한글은 WebP 를 못 읽는다** — 그대로 넣으면 파일은 열리고
    배치도 맞는데 그림 자리마다 **깨진 아이콘**만 나온다. `toHwpImage()` 가 캔버스로
    다시 그려 사진은 JPEG(품질 0.9), 로고는 PNG(투명 유지)로 바꾼다. 인쇄용이라
    긴 변 1200px 이면 충분해서 같이 줄인다 (926KB → 55KB).
    `HWP_IMAGE_MEDIA` 에 webp 를 넣지 말 것 — 테스트가 막는다.
  · 맨 아래 **로고도 그림으로 들어간다** (`LOGO_ITEM_ID`). 로고가 없을 때만 기본 문구를 싣는다.
  · **`header.xml` 의 `binDataList` 를 빠뜨리면 파일은 열리는데 사진만 통째로 사라진다.**
    잇는 열쇠는 id 가 아니라 **BinData 파일이름(확장자 뺀 것)** 이다 (`binaryItemIDRef="BIN0001"`).
    실제로 이걸 빠뜨렸다가 되읽기 검사에서 잡았다 — 눈으로는 안 보이는 구멍이다.
  · 회귀는 `scripts/hwpx.test.mjs` 가 막는다 (위 조건을 그대로 검사한다).
- 한글 원본을 올리는 **첨부(판) 기능은 그대로 남겼다** — 아래 항목 참고.

### 문서 첨부 — 갈래로 나누고 '판' 으로 쌓는다

A 가 올리고 → A 가 고쳐 올리고 → B 가 또 고쳐 올리는 게 실제 흐름이다.
평면 목록으로 쌓으면 `지도안_최종2.hwp` 가 여러 개 생겨서 **뭐가 최신이고 누가 뭘 바꿨는지
아무도 모른다** (카톡과 똑같은 문제). 그래서 **문서 하나 = 카드 하나**로 두고 판을 쌓는다.

- 같은 문서의 판들은 `plan_files.group_id` 가 같고 `version` 으로 줄을 선다.
  새 문서는 자기 `id` 를 `group_id` 로 삼는다 (판을 쌓을 기준점).
- `member_id`(올린 사람) · `note`(이번에 바꾼 것)를 같이 남긴다.
  **예전엔 누가 올렸는지조차 안 남았다** — 지적사항엔 이름이 남는데 계획안엔 없었다.
- 카드에는 늘 **최신 판만** 크게. `지난 판 N개` 는 접혀 있고 펼치면 판별로
  이름·시간·바꾼 내용이 보이고 그대로 내려받을 수 있다.
- **`최신 전체 받기`** 는 문서별 최신 판만 zip 으로 묶는다. 지난 판까지 다 주면
  받는 쪽이 또 헷갈린다.
- 최신 판을 지우면 **그 아래 판이 다시 최신**이 된다 (문서가 사라지지 않는다).
- 인쇄물에도 문서별 최신 판만 싣고 판 번호·올린 사람·바꾼 내용을 적는다.
- 파일 이름은 판마다 달라도 된다 (`지도안.hwp` → `지도안_보완.hwp`). 묶는 기준은 이름이 아니라
  `group_id` 라서, 이름이 바뀌어도 같은 문서로 남는다.

#### 갈래 — 무엇을 올린 건지 (`plan_files.kind`)

**웹앱마다 강사교육용 교육안·양식을 올리려고** 만들었다. 새 메뉴를 만들지 않았다 —
이 자리가 이미 판 쌓기·미리보기·zip 받기를 다 갖춘 문서함이라, **갈래 칸 하나만** 더했다.

| 갈래 | 무엇 | 누가 읽나 |
|---|---|---|
| `plan` **계획안** *(기본값)* | 프로그램 계획 문서 | 우리끼리·학교 |
| `guide` **교육안** | 이 웹앱 수업을 **어떻게 진행하는지** | **강사** |
| `form` **양식** | 활동지·학습지 — 인쇄해 나눠준다 | 학생 |
| `etc` **기타** | PPT·영상·참고자료 | — |

- **형식이 아니라 '누가 읽는가' 로 가른다.** 한글이냐 PPT냐로 가르면 확장자 라벨
  (`FileBadge`)과 겹치기만 하고 아무것도 안 알려준다.
- **기본값이 `plan` 인 이유**: 이 칸이 생기기 전에 올린 파일은 전부 계획안이었다.
  다른 값을 기본으로 두면 **지난 파일의 뜻이 소급해서 바뀐다.**
- **올릴 때 갈래를 고르기 전에는 버튼이 안 눌린다.** 미리 골라두면 여러 개가 한꺼번에
  엉뚱한 갈래로 들어가고, 되돌리는 것도 그만큼 여러 번이다 (`TaskAssign` 과 같은 판단).
- **있는 갈래만 칩으로 그리고, 갈래가 하나뿐이면 줄 자체를 안 그린다.**
  0건 갈래로 걸러봐야 '없어요' 만 나오는데 44px 을 먹는다.
- `최신 전체 받기` 는 **화면에 보이는 것만** 묶는다 — 갈래로 걸러놓고 전부 받으면
  화면과 다른 게 온다. 파일 이름도 갈래를 따라간다 (`제과제빵_교육안.zip`).
- **이름은 사람이 짓지 않는다.** 프로그램 페이지 안이라 프로그램 이름이 이미 제목이고,
  `/apps` 가 이미 주제별 트리다 — "프로그램 종류별" 이 공짜로 된다.
  올린 파일 이름도 그대로 둔다 (이름 규칙을 강제하면 올리기 전에 파일명을 고쳐야 해서 안 올린다).
- 인쇄물에서는 색 칩이 안 보이니 **갈래를 글로** 앞에 적는다 (`[교육안] 파일이름`).
- 갈래 목록은 `src/lib/types.ts` 의 `PLAN_KINDS` 하나다 (`FINDING_META` 와 같은 꼴).
  **넷에서 멈춘다** — 늘리면 올릴 때마다 고민이 늘고 375px 에서 칩이 두 줄로 접힌다.

> **왜 `강사교육` 메뉴를 따로 안 만들었나.** 프로그램 페이지가 바로 *"따로국밥을 없앤 곳"*
> 이라서다. 교육안만 다른 메뉴로 빼면 그 결정을 거스른다 — 제과제빵 수업을 나가는 강사는
> 제과제빵 한 페이지만 열면 된다. 나중에 *"이번에 나갈 자료 어디"* 를 자주 헤매면
> 그때 모아보는 화면을 얹으면 되고, **갈래가 이미 있어서 화면만 얹으면 된다**
> (`/verify` 가 새 표 없이 만들어진 것과 같은 방식). 계획은 `docs/training-guide-naming.md`.

### 업무 = 누가 · 무엇을 · 언제까지

> **"강사 역할을 하세요" 로는 아무도 움직이지 않는다.** 스스로 할 일을 찾아내길 기다리다
> 아무것도 안 굴러갔다. 무엇을 · 누가 · 언제까지가 **한 줄로 적혀 있어야** 일이 된다.
> 그 한 줄이 `moalab.tasks` 한 행이다.

- **담당자는 한 명**(`assignee_id`)이다. '분업' 이 목적이라 책임이 한 사람에게 있어야 한다.
  여럿이 할 일은 **사람마다 한 줄씩** 만든다 — *다 같이 할 일은 아무도 안 한다.*
- **상태는 담당자가 직접 누른다** (`todo` 할 일 / `doing` 하는 중 / `done` 완료).
  앱 상태(`computeStatus`)와 달리 계산할 하위 데이터가 없다. 강사양성의 3단 버튼과 같은 모양이다.
  · **완료 확인 단계는 두지 않았다.** 원장이 한 번 더 확인하게 하면 원장 할 일이 늘고,
    확인이 밀리면 목록이 완료로 꽉 찬다. 나중에 컬럼 하나로 얹을 수 있다.
- **프로그램에 걸 수도, 안 걸 수도 있다** (`app_id` 는 선택). 학교 제안서·재료 주문처럼
  프로그램과 무관한 일이 실제로 더 많다.
- 사람을 지워도 **업무 기록은 남는다** (`on delete set null`). 지적사항과 같은 갈래다 —
  `app_reviewers` 처럼 `cascade` 로 두면 사람을 지울 때 그 사람이 한 일이 통째로 사라진다.
- 목록 순서 = **급한 순서**다: 지난 것(오래된 순) → 오늘 → 가까운 순 → 기한 없음 → 완료.
  끝낸 것은 `<details>` 로 접는다 (프로그램 목록의 `검증 완료 보관함` 과 같은 판단).
- **빈 묶음은 아예 안 그린다.** '오늘' 이 0건이면 그 머리글도 없다.
- **묶는 축이 두 개다** (`급한 순` / `사람별`, 고른 것은 기기에 기억).
  급한 순은 *언제까지* 를, 사람별은 *누가 뭘 맡았나* 를 보여준다. **원장이 실제로 하는 일은
  나누는 것**이라 나눈 결과가 사람 단위로 보여야 한다. 계산은 `groupByPerson`.
  · **담당자 미정이 늘 맨 위**다 — 그게 채워야 할 것이다
  · **업무 0건인 사람 칸은 안 그린다.** 빈 칸이 쌓이면 스크롤만 길어진다
  · 지워진 멤버에게 걸린 업무도 맨 뒤에 모아둔다 — 어디에도 안 실리면 영영 못 찾는다
  · 사람별로 볼 땐 `사람별 남은 업무` 카드를 **안 그린다.** 바로 아래에 같은 것이
    펼쳐져 있어서 같은 숫자를 두 번 읽게 하고 300px 을 먹는다
  · '내 업무' 탭은 전부 내 것이라 축을 고를 게 없다 — 토글도 안 그린다
- **원장은 `전체` 로 시작한다.** 원장은 나눠주는 쪽이라 자기 '내 업무' 가 비어 있는 날이 많은데,
  거기서 시작하면 담당자 미정이 5건 있어도 **빈 화면**을 본다 (홈에서 원장이 전체 현황부터
  보는 것과 같은 판단). 강사는 토글 자체가 없어 늘 '내 업무' 다.
- 업무를 배정하면 **그 사람에게만** 알림이 간다 (`sendPush({ memberIds: [담당자] })`).
  · 이게 **`memberIds` 를 실제로 쓰는 첫 자리**다. 지금까지 알림은 전부 전원에게 갔다
    (서버는 `route.ts` 에서 이미 지원하고 있었는데 부르는 쪽이 안 썼다).
  · 제목만 고쳤을 땐 안 울린다 — **담당자가 바뀌었을 때만** 울린다.

**⚠️ 강사양성(`training_*`)과 합치지 않았다.** 축이 다르다.
저쪽은 *전원 × 고정 커리큘럼* 매트릭스라 기한이 없고 목록이 영구적이다.
이쪽은 *1건 × 담당자 1명 × 기한* 이고 끝나면 지나간다.
합치면 이수율 판도 기한 알림도 둘 다 못 쓴다 — **원가 vs 지출을 나눈 것과 같은 이유**다.
이 판단은 반드시 다시 제기되니 여기 적어둔다.

### 역할분장 = 부서 › 중분류 › 소분류 · 그리고 담당자

> **업무(`tasks`)와 축이 다르다.** 저쪽은 *1건 × 담당자 1명 × 기한* 이고 끝나면 지나간다.
> 이쪽은 **기한이 없고 계속 남는 '이 일은 누구 담당'** 이다.
> 합치면 기한 알림도 못 쓰고 역할표도 못 쓴다 —
> **원가 vs 지출 · 업무 vs 강사양성 을 나눈 것과 같은 이유**다. 이 판단은 다시 제기되니 적어둔다.

- **3단이다.** 부서(`departments`) › 중분류(`duty_groups`) › 소분류(`duties`).
  소분류 하나가 사람이 실제로 맡는 단위다.
- **주담당 1명**(`duties.owner_id`) + **부담당 여럿**(`duty_helpers`).
  · 책임은 한 사람에게 지운다 (업무의 `assignee_id` 와 같은 판단). 둘이 나눠 맡아야 하면
    역할을 둘로 쪼갠다 — 화면에도 그렇게 적어뒀다
  · **주담당으로 고른 사람은 부담당에서 자동으로 빠진다.** 안 그러면 사람별 보기에서
    한 사람이 두 번 세어져 숫자가 어긋난다 (`buildOrg` 와 `DutyForm` 양쪽에서 거른다)
- **사람을 지워도 역할은 남는다** (`owner_id` 는 `set null` → 담당자 미정으로 돌아간다).
  부담당은 `cascade` 로 같이 지운다 — 주담당과 달리 남겨봐야 뜻이 없다.
- 보기 세 가지 — **원장은 `부서별`, 강사는 `내 역할`** 로 시작한다.
  원장은 나누는 쪽이라 자기 역할부터 보면 정작 미정이 안 보인다 (업무 화면과 같은 판단).
- **`담당자 미정` 이 늘 맨 위다** — 그게 채워야 할 것이다. `담당자 미정 N건만 보기` 버튼도 뒀다.
- **역할 0건인 사람도 그린다.** 업무에서는 0건인 사람 칸을 안 그렸는데 **여기서는 반대**다 —
  *아무것도 안 맡은 사람이 있다는 것 자체가 봐야 할 정보*다.
- **두 단계가 다 접힌다.** 부서만 접으면 부서 하나를 열었을 때 소분류가 전부 쏟아져
  1499px 이 됐다. 중분류도 접이식으로 바꿔 **1031px** 이 됐다.
  · 부서 전부 접힘 = **812px, 정확히 한 화면** (부서가 5개든 20개든)
  · **중분류가 하나뿐인 부서는 열어둔다** — 누를 게 없는 단계를 만들지 않는다
  · 왼쪽 세로선(`border-l`)으로 트리 층을 그린다. 들여쓰기가 없으면 중분류가
    부서와 같은 층으로 읽힌다 (높이는 안 늘어난다)
- **검색이 걸리면 저절로 펼친다** — `Collapsible` 의 `forceOpen`.
  `defaultOpen` 으로는 **안 된다**: `useState(defaultOpen)` 은 첫 값만 잡아서
  나중에 검색어가 들어와도 안 열리고, 기억해둔 상태가 있으면 그것이 이긴다.
  실제로 그렇게 만들어놓고 "저절로 펼친다" 고 적어뒀는데 **동작하지 않았다.**
- **`담당자 미정만 보기` 는 트리를 안 그린다.** 평평한 한 줄 목록이다 —
  트리로 그리면 부서·중분류를 전부 펼쳐야 해서 15건 보는 데 2949px(3.6화면)이었다.
  채우는 게 목적인 화면이라 헤집을 이유가 없다 (1164px).
- **알림을 안 보낸다.** 역할에는 기한이 없어 지금 당장 움직일 일이 아니고,
  원장이 미정 12건을 한 번에 채우면 12통이 나간다 — 그날로 알림을 꺼버린다
  (체크리스트 뿌리기에서 '사람당 한 통' 으로 묶은 것과 같은 이유).
- **권한은 `/apps` 와 같은 갈래다.**
  · **역할(소분류) 추가·수정·담당자 지정은 전원**. 원장만 두면 등록이 밀린다
    (`+ 새 앱` 을 전원에게 연 것과 같은 이유). *"이건 내가 할게요"* 를 본인이 누른다 —
    검증자 참여를 본인이 누르게 연 것과 같은 갈래다
  · **역할 지우기 · 부서·중분류(조직의 틀)는 원장만.** 한 사람이 누르면 모두의 역할표
    모양이 바뀐다 (주제 관리·보관과 같은 갈래). 남이 적어둔 역할을 없애는 것도 여기 든다
  · 강사도 전체를 볼 수 있다 — *"이건 누구한테 물어보지"* 가 이 화면의 쓸모라 감춰봐야 손해다
- 부서·중분류·소분류는 **전부 데이터다.** 조직이 바뀔 때 코드를 안 고친다 (`topics` 와 같은 판단).
  예시는 `supabase/seed-org.sql` 에 따로 뒀다 — `schema.sql` 에 넣으면 원장이 지운 줄이
  스키마를 다시 돌릴 때마다 되살아난다.
- 계산은 전부 `src/lib/org.ts` 에 모여 있고 `scripts/org.test.mjs` 가 지킨다.

> **왜 `Role` 이 아니라 `Duty` 인가.** `src/lib/types.ts` 에 `Role = 'admin' | 'teacher'` 가
> 이미 있다. 같은 낱말이 두 뜻이 되면 반드시 사고가 난다.

### 체크리스트 — 매번 비슷한 일을 버튼 한 번으로

나눠주는 일은 매번 비슷하다. 한 벌(`task_templates` + `task_template_items`)을 만들어두고
**기준일만 정하면 통째로 뿌린다**. 항목마다 `day_offset`(기준일 대비 며칠)을 적어두면
기한이 자동으로 계산된다 — 기준일이 수업날이면 준비는 `-5`, `-2` 처럼 앞에 온다.

- **저장 전 미리보기가 필수다.** 여러 건이 한꺼번에 생기는 동작이라 잘못 뿌리면
  지우는 것도 여러 번이고 **엉뚱한 알림까지 나간다.** 미리보기에서 담당자를 줄마다 바꿀 수 있다.
- **알림은 사람당 한 통**(`spreadNotices`). 8건 뿌렸는데 8통이 오면 그날로 알림을 꺼버린다.
- 저장은 **insert 한 번**(배열). 중간에 반만 들어가는 일이 없다.
- **뿌린 업무는 그 순간 사진처럼 굳는다.** 템플릿을 고쳐도 이미 뿌린 건 안 바뀐다 —
  그래서 `tasks` 에 `template_id` 를 두지 않았다. 지난달에 끝낸 일이 슬그머니 달라지면
  기록이 아니게 된다.
- 뿌린 뒤 **묶음 통째로 삭제**(`delete where batch_id`)와 **묶음 전체 N일 미루기**를
  같은 화면에 뒀다. 한 번에 뿌린 것은 한 번에 물릴 수 있어야 한다.
  · 미룰 때 **끝난 업무와 기한 없는 업무는 안 건드린다** (지나간 기록의 날짜를 바꿀 이유가 없다)
- 체크리스트를 만들고 고치는 건 **원장만**. 뿌리는 것도 원장만이다.

### 나눠주기 — 체크해서 한 사람에게 한꺼번에

말로 넣기·체크리스트로 뿌리기는 업무를 여러 건 만들어놓고 **담당자를 비워두는 일이 흔하다.**
그걸 하나씩 `[⋯ → 고치기 → 담당자 → 저장]` 으로 채우면 5건에 스무 번을 누른다 —
**`주제로 옮기기` 와 똑같은 문제라 똑같이 푼다** (`src/components/TaskAssign.tsx`).

- 카드가 아니라 **한 줄짜리 목록**이고, 지금 담당자별로 묶여 있다. **담당자 미정이 맨 위.**
- 저장은 `update ... in (...)` **요청 하나**. 알림은 **사람당 한 통**(뿌리기와 같은 규칙).
- **처음부터 사람이 골라져 있으면 안 된다.** 목록 맨 위가 원장 자신이라, 체크하고 옆의
  넘기기를 그냥 누르면 전부 자기한테 오고 **알림까지 나간다.** 그래서 `''`(아직 안 고름)를
  따로 두고 고르기 전에는 버튼이 안 눌린다. 되돌리기는 `CLEAR` 라는 별도 값이다.
- 들어가는 자리가 셋이다 — `나눠주기` 칩(미정 건수를 배지로) · `사람별 남은 업무` 카드의
  미정 줄 · 사람별 보기의 `담당자 미정` 머리글. 셋 다 같은 시트를 연다.

계산은 전부 `src/lib/task.ts` 에 모여 있고 `scripts/task.test.mjs` 가 지킨다.
**서버에서 '오늘' 을 만들 땐 반드시 `kstDateStr()`** 를 쓴다 —
Vercel 은 UTC 로 돌아서 그냥 짜면 한국 시간 아침 9시 전까지 어제 날짜가 나온다
(기한 알림이 하루씩 밀리는 가장 흔한 버그).

### 말로 업무 넣기 — 원장만

원장이 말로 던진 지시를 업무 여러 건으로 쪼갠다. **원장 한 사람만 쓴다.**

- **음성인식은 앱이 하지 않는다.** 큰 텍스트 칸을 두고 **폰 키보드의 마이크**를 그대로 쓴다.
  · 아이폰·안드로이드 둘 다 확실하고, 잘못 들었을 때 그 자리에서 고칠 수 있다
  · 앱 안에 마이크 버튼(Web Speech)을 넣으면 **아이폰 홈 화면 앱에서 막히는** 경우가 있다
- 쪼개는 건 **Claude API** (`/api/task/parse`). 오늘 날짜(`kstDateStr`)와 멤버 목록을
  **서버에서** 박아 넣는다 — 브라우저가 보낸 걸 그대로 쓰면 남의 이름으로 업무를 만들 수 있다.
- **모델은 원장이 고른다** (`관리 > AI 키`, `src/lib/ai.ts` 의 `AI_MODELS`).
  이 일은 어려운 축이 아니라 **가장 싼 것으로도 충분하다** — 화면에 한 번 말할 때 얼마인지 적어뒀다.
  기본값은 `claude-opus-5` 이고, 저장된 값이 목록에 없으면 `resolveModel()` 이 기본값으로 되돌린다
  (모델 이름이 바뀌어도 화면이 안 죽게).
- **AI 가 준 것을 그대로 믿지 않는다** (`src/lib/taskParse.ts` → `normalizeParsed`).
  · 지금 멤버 id 가 아니면 **담당자 미정**으로 돌린다 (줄은 버리지 않는다 — 화면에서 고르면 된다)
  · `2026-02-30` 같은 **없는 날짜**는 기한 없음으로 돌린다 (평년 2/29 도 걸러진다)
  · 제목이 없는 줄은 버리고, 최대 20건에서 자른다 (응답이 폭주해도 수백 건이 안 생긴다)
- **저장 전 미리보기 필수.** 제목·담당자·기한을 줄마다 고치고 뺄 수 있다.
  알림은 뿌리기와 같은 규칙 — **사람당 한 통**.
- 묶음 이름을 두면 나중에 **통째로 지우거나 미룰 수 있다.** AI 가 만든 것이라 더 그렇다.

**⚠️ API 키는 `moalab.app_secrets` 에 있고 PIN 과 똑같이 잠겨 있다.**
브라우저는 그 표에 아예 못 붙고, 서버도 **끝 4자리만** 돌려준다.
등록·삭제는 `관리` 화면의 `AI 키` 카드 → `/api/settings/ai` (원장 확인 후 처리).
이 표를 `internal_all` 배열에 넣으면 **키가 통째로 새어 나간다** — 위 스키마 항목 참고.

### 검증 라운드

앱을 수정하면 이전 검증은 무효다.

- 앱 등록 → 1차 라운드 자동 생성
- 앱 상세의 **"수정했음 → 재검증 요청"** → 수정 내용 입력 필수 → **N+1차 라운드**
- 지적과 '검증 완료' 표시는 **라운드에 붙어 있어서** 새 라운드는 자동으로 백지에서 시작한다
- 이전 라운드는 접힌 상태로 보존 (누가 언제 뭘 지적했고 어떤 답이 달렸는지 캡처까지 남는다)

관련 코드는 전부 `src/lib/verify.ts` 에 모여 있다.

### 상태 자동 계산

앱 `status` 는 **사람이 고르지 않는다.** 현재 라운드 체크 결과로만 결정된다
(`src/lib/status.ts` → `computeStatus`).

| 조건 | 상태 | 색 | 다음에 움직일 사람 |
|---|---|---|---|
| 답 안 한 지적(`open`)이 하나라도 있으면 | `fixing` 수정 필요 | 빨강 | 제작자 |
| `fixed`·`recheck` 만 남았으면 | `recheck` **다시확인** | 노랑 | 검증자 |
| 지적 0건 + **검증자 전원이 '검증 완료'** | `done` 검증 완료 | 초록 | — |
| 그 외 | `pending` 진행 중 | 회색 | 검증자 |

**`fixing` 과 `recheck` 를 갈라놓은 이유**: 둘 다 "안 닫힌 지적이 있다" 는 같지만
*다음에 움직여야 할 사람이 다르다*. 한 덩어리로 두면 목록에서 전부 '수정 필요' 로 보여서
**이미 고쳐놓고 확인만 기다리는 것과 아직 손도 안 댄 것이 구분되지 않았다.**
지적 답변의 '다시확인' 과 같은 amber 색을 쓴다 — 같은 것을 가리키니 색도 같아야 한다.
`scripts/status.test.mjs` 가 이 표를 그대로 검증한다 (`open` + `recheck` 가 섞이면 반드시 `fixing`).

지적이 0건이라고 바로 `done` 이 되면 **"아무도 안 봤다" 와 "다 봤는데 문제없다" 가
구분되지 않는다.** 그래서 검증자의 명시적인 **검증 완료**(`moalab.round_signoffs`)를 요구한다.
진행률은 `검증 완료 누른 사람 / 검증자 수` 다.

- 배정에서 빠진 사람의 낡은 `round_signoffs` 는 **세지 않는다** (정족수가 부풀려진다).
  검증자를 바꿀 때 `AppForm` 이 지우고, 화면과 `recomputeAppStatus` 양쪽에서 한 번 더 걸러낸다.
  · `recomputeAppStatus` 는 예전에 이 거르기가 빠져서 `count` 를 그냥 셌다. 그래서
    **화면엔 0/2 인데 DB 의 `status` 만 `done`** 이 되는 어긋남이 생길 수 있었다.
- `apps.status` 에는 `pending`·`fixing`·`done` **세 값만** 들어간다 (DB `check` 제약).
  예전 코드가 지적 상태(`recheck`)를 그대로 써 넣은 행이 있었는데, 그 값은 `STATUS_META` 에
  없어서 인쇄 화면이 죽었다. 앱 상태와 지적 상태는 이름이 겹쳐도 **다른 집합**이다.
- 지적을 쓰거나·답하거나·닫을 때마다 `recomputeAppStatus(appId)` 가 DB 의 `status` 를 다시 쓴다.

### 푸시 알림

`src/lib/push.ts` + `public/sw.js` + `/api/push/send`

- 표준 Web Push (VAPID). 구독은 `moalab.push_subscriptions`, **브라우저마다 한 줄**
  (`endpoint` 가 고유키). 폰에서 켜도 PC 는 따로 켜야 한다.
- **알림이 나가는 순간** — 새 공지 / 새 지적 / 지적 답변 / 업무 배정 /
  **매일 아침 기한 알림**(`/api/cron/due`).
  `fromId` 를 보내서 **자기가 한 일은 자기한테 안 울린다.**
- **`sendPush()` 는 결과를 돌려주지만 안 받아도 된다.** 그냥 부르면 예전처럼 던지고 잊는 것이고,
  몇 명에게 갔는지 화면에 적어야 할 때만 `await` 한다 (공지 저장이 그렇다).
  · 예전엔 결과를 알 방법이 없어서 **키가 없어도, 구독자가 0명이어도** "알림도 보냈어요" 가 떴다
- 발송이 실패해도 본 작업(공지 저장 등)을 막지 않는다. `sendPush()` 는 fire-and-forget 이고
  서버도 실패 시 200 + `skipped` 를 돌려준다. **알림 때문에 공지가 안 올라가면 더 나쁘다.**
- 구독이 죽으면(404/410) 발송하면서 그 줄을 지운다.
- 서비스 워커는 **캐싱을 하지 않는다.** 오프라인 대응까지 하면
  "고쳐서 배포했는데 옛 화면이 뜬다" 는 더 큰 문제가 생긴다.
- **아이폰은 사파리에서 '홈 화면에 추가' 를 해야 알림을 받는다** (iOS 16.4+).
  그래서 지원 안 되는 걸 그냥 숨기지 말고 `PushToggle` 이 그 이유를 한글로 적어준다.
- **`PushToggle` 은 홈에 있다.** 예전엔 원장 전용 `관리` 화면에만 있어서
  **강사는 알림을 켤 방법이 아예 없었다** (그래서 아무도 못 켰다).
  대신 **이미 켜져 있으면 한 줄로 접는다** — 다 켠 사람 홈에서 큰 카드가 계속 자리를 먹으면 안 된다.
  (`관리` 에도 그대로 둔다 — 원장이 늘 보던 자리다)
- 환경변수 3개. 없으면 알림 기능만 조용히 꺼지고 나머지는 그대로 돌아간다.
  `NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT`
  (`npx web-push generate-vapid-keys` 로 만든다. private 키에 `NEXT_PUBLIC_` 붙이지 말 것)

### 기한 알림 — 매일 아침 한 번

`vercel.json` 의 cron 이 `/api/cron/due` 를 **UTC 23:00 = 한국 아침 8시**에 부른다.
오늘·내일 마감인 업무를 **담당자별로 묶어 그 사람에게만** 보낸다.

- **Vercel 무료 플랜은 cron 이 하루 1회**라 "전날 저녁 + 당일 아침" 두 번을 못 쓴다.
  그래서 아침 한 통에 `오늘 2건 · 내일 1건 — A초 제안서 외 2건` 을 같이 담는다.
  통수가 줄어서 이쪽이 오히려 낫다. (정확히 8시가 아니라 8시대 안 어느 시점에 울린다)
- **`CRON_SECRET` 이 없으면 401 로 막는다.** 이 주소는 누구나 부를 수 있어서
  비워두고 여는 건 위험하다. Vercel 은 이 환경변수가 있으면 헤더를 알아서 붙여준다.
- **보낸 뒤 `tasks.reminded_on` 에 오늘 날짜를 적는다** — cron 이 두 번 돌거나
  손으로 한 번 더 불러도 같은 사람에게 두 통이 가지 않는다.
  · 알림을 안 켠 사람 몫도 '보낸 것' 으로 표시한다. 안 그러면 매일 같은 줄을 다시 시도한다.
- **⚠️ 날짜는 반드시 `kstDateStr()` 로 만든다.** 서버의 '오늘' 은 한국 날짜가 아니다 —
  그냥 짜면 한국 시간 아침 9시 전까지 어제 날짜가 나와서 **알림이 하루씩 밀린다.**
- 손으로 확인: `curl -X POST -H "authorization: Bearer <CRON_SECRET>" <주소>/api/cron/due`
  → `{today, people, tasks, sent, removed}` 를 돌려준다.

### 인쇄 / PDF 저장

`/print/[id]` — 프로그램 화면의 **인쇄 / PDF 저장** 에서 넣을 것을 체크해서 연다.
`?parts=verify,plan,cost,sample,photo` 로 무엇을 넣을지 정한다 (`src/lib/print.ts`).

**역할분장 인쇄**는 `/roles` 상단의 인쇄 버튼 → `/print/roles?parts=dept,person,unassigned`
- `dept` **부서별 통합** — 조직 전체가 한 문서에 (부서 › 중분류 › 역할 › 주담당·부담당).
  중분류 이름은 그 묶음 첫 줄에만 적는다 — 같은 이름이 반복되면 표가 시끄럽다.
  부서마다 새 쪽으로 가르지 **않는다** — 부서 5개가 각각 반 쪽이면 종이가 두 배다.
- `person` **사람별** — **한 사람 = 한 쪽.** 뽑아서 그 사람에게 그대로 건네는 게 이 장의
  쓸모라 본인·원장 서명란까지 넣었다. **역할 0건인 사람은 쪽을 안 내주고** 맨 아래 한 줄로
  모은다 (화면에서는 0건인 사람도 그린다 — 종이는 장수가 곧 비용이라 판단이 다르다).
- `unassigned` **담당자 미정만** — 미정이 48건이라 이것만 뽑는 일이 잦다.
  회의에서 이 종이를 놓고 정하는 게 실제 흐름이라 **서명란**을 옆에 뒀다.
- 미정 표를 같이 뽑으면 사람별에서 `담당자 미정` 장을 뺀다 — 같은 목록이 두 번 실린다.
  미정 표를 안 뽑을 때는 남겨서 어디에도 안 실리는 일이 없게 한다.
- **같은 값이 이어지는 칸은 통합 셀로 합친다** (`mergeRuns` → `rowSpan`). 부서별의 중분류,
  사람별·미정의 부서와 중분류가 그렇다. 예전엔 첫 줄에만 적고 나머지를 빈 칸으로 뒀는데
  **빈 칸도 칸이라 줄마다 가로선이 그어져서 묶음이 안 보였다.**
  · **연속한 것만** 합친다 — 떨어져 있는 같은 값을 합치면 표에 없던 순서가 생긴다
  · 중분류는 **부서가 같을 때만** 합친다 (`부서 › 중분류` 를 열쇠로 쓴다).
    부서가 다른데 이름이 우연히 같다고 합치면 서로 다른 부서가 한 칸에 묶인다
  · 가로선을 `<tr>` 이 아니라 `<td>` 마다 그린다 — `<tr>` 에 그으면 병합 칸을 가로질러 갈린다
  · `scripts/print.test.mjs` 가 지킨다. **한 칸이라도 틀리면 그 아래 열이 전부 밀리는데
    눈으로는 늦게 발견된다** — rowSpan 합 == 줄 수, 덮는 범위에 빈틈·겹침 없음을 못박아 뒀다

목록 단위 인쇄는 **프로그램계획 상단의 인쇄 버튼**에서 연다:
- `/print/lectures` — **강의계획서 전체 인쇄.** 목표를 쓴 프로그램 전부를
  한 프로그램 = A4 한 장(fill)으로 잇는다. 순서는 목록 트리와 같다(주제 → 이름).
  안 쓴 프로그램은 안 싣는다 — 빈 양식이 섞이면 덜 만든 문서처럼 보인다.
  몇 개가 빠졌는지는 화면(no-print)에서만 알려준다.
- `/print/status` — **검증 현황 표.** 검증 완료 / 미완료 두 표로 나누고,
  미완료 표에는 상태(수정 필요·다시확인·진행 중)를 실어 다음에 누가 움직여야
  하는지 종이에서도 보이게 한다. 상태는 목록과 같은 `useAppsOverview` 재계산 값이다.

- **PDF 라이브러리를 쓰지 않는다.** 브라우저 인쇄를 그대로 쓴다.
  · 아이폰: 사파리 공유 → 프린트 → 두 손가락으로 벌려 PDF 저장
  · PC: 인쇄 대화상자에서 대상을 'PDF로 저장'
  라이브러리로 그리면 한글 폰트를 따로 심어야 하고 표가 깨진다.
- 인쇄용 CSS 는 `globals.css` 의 `@media print` 한 곳에 모여 있다.
  `.no-print`(조작 줄 숨김) / `.print-block`(페이지 경계에서 안 잘림) / `@page A4`.
- **강의계획서는 양식 그대로** 표로 그린다 (`PlanSheet`).
  · **강의계획서만 골랐으면 표지를 안 붙인다** (`onlyPlan`). 표지는 여러 묶음을 한 문서로
    묶을 때 쓰는 머리다. 학교에 계획서 한 장만 낼 때 우리 내부 검증 현황·제작자가
    같이 찍히면 안 된다. 인쇄 고르기 화면에 `강의계획서 한 장만` 버튼을 뒀다.
  · 쪽 넘김도 앞에 뭔가 있을 때만 넣는다 — 한 장만 뽑는데 넣으면 빈 쪽이 딸려 나온다.
    묶음 인쇄에서는 계획서 **뒤에도** 쪽을 가른다(`print-page-break-after`) — 장 높이가
    268mm 라 뒤 섹션이 남은 1mm 틈으로 파고들면 안 된다.
  · 계획서는 **늘 A4 한 장**이다 — 사진 칸 높이를 `PlanSheet` 가 스스로 잰다
    (위 "강의계획서" 항목 참고).
- **첨부파일은 목록만** 들어간다. 한글·PDF 본문을 이 문서에 넣을 방법이 없어서
  파일 이름·크기만 싣고 그 사실을 문서에 적어둔다.
- 원가표는 표로 다시 그린다 (품목·구분·묶음가·1인당·총액 + 합계·판매가·마진).
- 이 화면은 `(app)` 레이아웃 밖이라 **로그인 가드를 직접 붙였다.**
- 상수를 `src/lib/print.ts` 로 뺀 이유: Next.js page 파일은 컴포넌트 외의 export 를 허용하지 않는다.

### 로고 · 앱 아이콘

원본은 `assets/moakit-logo-source.png` (검정 배경의 모아킷 로고).
여기서 `scripts/make-icons.mjs` 가 전부 뽑아낸다.

```bash
npm i -D playwright && node scripts/make-icons.mjs
```

playwright 는 아이콘을 다시 만들 때만 필요해서 `package.json` 에 넣지 않았다.
**로고가 바뀌면 원본만 갈아끼우고 이 스크립트를 다시 돌리면 된다.**

| 파일 | 쓰는 곳 |
|---|---|
| `public/moakit-symbol.png` | `BrandMark` — M 심볼만. **밝은 배경에도 쓸 수 있다** |
| `public/moakit-mark.png` | `BrandLogo` — 심볼 + moakit. PC 사이드바 |
| `public/moakit-logo.png` | `BrandLogo tagline` — 태그라인까지. 150px 이상에서만 |
| `public/icon-192·512.png` | PWA (`manifest.webmanifest`) |
| `public/icon-maskable-512.png` | 안드로이드 — 원형으로 잘려도 M 이 안 잘리게 작게 넣었다 |
| `public/apple-touch-icon.png` | 아이폰 홈 화면. **iOS 가 모서리를 스스로 깎으니 각지게** 준다 |
| `src/app/icon.png` | 파비콘 (app router 가 자동으로 집는다) |

- 배경 검정을 알파로 빼낼 때 `alpha = max(r,g,b)` 로 두고 **프리멀티플라이를 되돌린다.**
  안 그러면 글자 경계에 검은 테가 남는다.
- **전체 로고는 어두운 배경에서만.** `moa` 가 흰색이라 밝은 배경에서 사라진다.
  그래서 로그인 화면(밝음)에서는 **앱 아이콘과 같은 검은 판** 위에 심볼을 올렸다.
- UI 강조색은 그대로 브랜드 오렌지(`#F26522`)다. 로고 teal 로 통째로 바꾸지 않았다.

### 원가 계산

`src/lib/cost.ts`

```
소모품:   1인당 = (묶음가격 ÷ 묶음수량) × 1인사용량
          총   = 1인당 × 참여인원

재사용품: 1회당 = 묶음가격 ÷ 감가회차
          1인당 = 1회당 ÷ 참여인원
          총   = 1회당
```

- **강사비·교통비**처럼 수업 한 번에 통으로 나가는 돈은 `재사용 + 감가회차 1` 로 넣으면
  1회당 = 그 금액, 1인당 = 금액 ÷ 인원 이 된다.
- 인원수를 20 → 25 로 바꾸면 재사용품 1인당 단가가 **즉시** 내려간다.
  학교와 인원 협의할 때 이 숫자를 보고 판단한다.
- 인원·판매가는 화면에서 즉시 반영되지만 **저장은 명시적 버튼**이다.

### 월별 수익배분 = 직접비 우선 + 성과몫 + 남은 이익 1/N

`src/lib/revenueShare.ts` · `/revenue` · `/revenue/[appId]`

```
배분가능이익 = max(0, 실제 수금액 - 직접 운영비)
남은 기본 풀 = 배분가능이익 - 창작·사업계획서·영업 성과몫
개인 최종액 = 남은 기본 풀 1/N + 본인이 참여한 성과몫
```

- 팀은 기본 5명 전원이 1/N 대상이다. 성과 담당자도 성과몫을 받은 뒤 기본 1/N을 다시 받는다.
- 성과 항목에 두 명을 고르면 그 항목 금액만 1/2 한다. 세 명이면 1/3이다.
- 창작 기본값은 배분가능이익의 15%다. 영업·제안서는 사업이 커질수록 한계율이 내려가는
  **누진 체감 추천**을 쓰되, 합의하면 직접 입력으로 바꿀 수 있다.
  · 영업: 첫 300만원 15% · 다음 700만원 10% · 다음 2,000만원 7% · 초과 5%
  · 제안서: 첫 300만원 10% · 다음 700만원 7% · 다음 2,000만원 5% · 초과 3%
- 추천값은 법정·업계 표준이 아니라 내부 합의를 시작하기 위한 값이다.
- 보조·지원금은 총사업비에서 성공수수료를 떼는 계산으로 쓰지 않는다. 승인 비용·환수액을
  정산한 뒤 회사가 실제로 보유할 수 있는 자체 이익만 배분 기준액에 넣는다.
- 돈은 정수 원으로 계산하고 잔여 1원도 결정적인 순서로 나눠 총합이 항상 맞는다.
- 저장된 것은 현재 **배분 기준**이지 지급 원장이 아니다. 확정 정산·역분개는 별도 기능이다.
- 프로그램 기본안은 `revenue_share_plans`, 실제 월별 계산안은 프로그램×월 한 행인
  `revenue_share_months`에 나눈다. 새 달은 매출·비용 0원부터 시작하며 지난달을 자동 복사하지 않는다.
- 월 계산안은 `비율 미정 / 검토안 / 합의됨` 상태를 따로 저장한다. 사람별 금액은 어디까지나 예상액이며,
  실제 지급 확정·송금 원장 기능은 아직 없다.
- `/revenue`는 고른 달의 프로그램별 계산을 먼저 끝낸 뒤 같은 사람의 기본몫·성과몫을 합산한다.
  월 전체 이익에 누진 추천률을 다시 적용하지 않는다.
- 당시 멤버 이름과 전체 계산 결과를 월 행에 스냅샷으로 저장해, 팀 구성이 바뀌어도 지난달 표시가 사라지지 않는다.
- 계산 테스트는 `scripts/revenue-share.test.mjs`가 20%를 두 명이 나누는 예시, 여러 성과 중첩,
  원 단위 잔여, 적자, 300만·1,000만·3,000만원 누진 경계를 지킨다.

### 지출결의서 = 쓴 돈 한 줄 + 영수증

> **원가와 지출은 다른 것이다.** 원가(`cost_sheets`)는 *앞으로 얼마 들까*(계획),
> 지출(`expenses`)은 *실제로 얼마 썼나*(증빙)다. 섞으면 둘 다 못 쓴다.

영수증을 서랍에 모아두면 연말에 아무도 못 맞춘다. 그래서 **쓴 그 자리에서 폰으로**
네 가지만 넣는다 — **언제 · 얼마 · 무엇에 · 영수증 사진**. 나머지(프로그램·학교·비고)는
`더 넣을 것` 안에 접어뒀다. 필수를 늘리면 아무도 안 쓴다.

- 지출 한 줄 = `moalab.expenses` 한 행. 영수증은 `expense_files` 로 **여러 장** 붙는다
  (카드전표 + 간이영수증이 같이 오는 경우가 흔하다). 버킷은 `moalab-receipts`.
- **`사용 내용`(purpose) 은 필수다.** 회계가 실제로 읽는 칸이 이것뿐이다.
  금액·날짜만 있는 줄은 나중에 아무 의미가 없다.
- 구분 7개(`재료비·교통비·식비·도서·교구·사무·소모품·외주·용역비·기타`)는 회계 계정 이름에 맞췄다.
- 원장이 **확인**(`approved`)을 누른다. 강사는 자기 것만 수정, 원장은 전부.
- 금액 칸은 입력하는 동안에도 **콤마를 찍는다.** 폰에서 0 을 몇 개 쳤는지 눈으로 못 센다.
  숫자 아닌 글자는 버린다 (`4만5,000원` → `45000`).
- 달 계산은 `'YYYY-MM'` **문자열**로 한다 (`src/lib/expense.ts`). Date 로 왔다갔다 하면
  시간대 때문에 1일·말일이 앞뒤 달로 샌다. 연 경계·윤년은 테스트로 막아뒀다.

#### 달 단위 문서로 인쇄 — `/print/expense?month=2026-08`

목록 화면의 **인쇄** 에서 넣을 것을 체크해 새 창으로 연다
(`구분별 소계 · 날짜별 소계 · 지출 명세 표 · 영수증 첨부`).

- **명세 표의 번호(No.)와 영수증 사진의 번호가 같다.** 증빙은 "이 금액의 영수증이 이것" 이
  연결돼야 의미가 있어서, 번호로 묶는 게 이 문서의 핵심이다.
- 표지에 **결의자·확인 서명란**을 넣었다 (종이로 넘길 때 필요하다).
- **영수증 없는 지출은 문서 끝에 따로 모아 빨간 칸으로 싣는다.** 숨기면 그게 제일 늦게 터진다.
- 한글·PDF 첨부는 인쇄물에 본문을 실을 방법이 없어서 **파일 이름만** 적고 그 사실을 밝힌다.
- 프로그램 인쇄와 같이 **PDF 라이브러리를 쓰지 않는다** (브라우저 인쇄).
  이 경로는 고정 경로라서 빌드 때 미리 그려보려 하니 `Suspense` 로 감싸야 한다
  (`useSearchParams` 를 쓰기 때문).
- 목록에 걸어둔 **구분·사람 필터가 인쇄에도 그대로** 넘어가고, 그런 경우 문서에
  "일부만 뽑은 문서입니다" 를 박아둔다. 부분 문서가 전체로 오해되면 안 된다.

### 로그인 / 권한

- 회원가입 없음. 이름 버튼 → PIN 4자리. 세션은 localStorage 30일 (`src/lib/session.tsx`).
- **PIN 검증은 서버에서만** 한다 (`/api/login`, service_role 키).
  `members` 테이블은 RLS 로 anon 접근을 전면 차단했고,
  브라우저는 pin 컬럼이 없는 `members_public` 뷰만 읽는다.
- 멤버 추가/수정/삭제도 서버 라우트(`/api/members`)에서 `x-actor-id` 로 admin 을 확인한 뒤 처리.

#### 누가 무엇을 할 수 있나

| | 원장 | 강사 |
|---|---|---|
| **새 프로그램 등록** (`+ 새 앱`) | O | **O** |
| 프로그램 정보 수정 (링크·마감일·검증자…) | O | **제작자 본인만** |
| 재검증 요청 | O | 제작자 본인만 |
| 프로그램 보관 / 주제 관리 / 주제로 옮기기 | O | X |
| 계획안 첨부·강의계획서·샘플·원가·댓글 | O | O |
| **업무 만들기** | O | **O** (담당자 기본값은 자기 자신) |
| 업무 고치기·상태 바꾸기 | O | **담당자 본인 또는 만든 사람** |
| **업무 나눠주기** (체크 → 한 사람에게 한꺼번에) | O | X |
| **역할분장** — 부서·중분류·역할 만들기·고치기 | O | **O** |
| 역할 담당자 지정 (내가 이거 할게요) | O | **O** |
| 역할분장 **지우기** (부서·중분류·역할) | O | X |
| 체크리스트 관리 · 뿌리기 · 묶음 삭제/미루기 | O | X |
| **말로 업무 넣기** · AI 키 등록/삭제 | O | X |
| 검증 지적·답변·검증 완료 | O | O |
| 공지 쓰기 · 멤버 관리 · 관리 화면 | O | X |
| **역할분장 보기** | O | **O** (전체 다 보인다) |
| **역할(소분류) 만들기·고치기·담당자 지정** | O | **O** |
| 역할 지우기 · 부서·중분류 만들기·고치기·지우기 | O | X |

- **프로그램 등록을 원장만 하게 두면 등록이 밀린다.** 만든 사람이 그 자리에서
  올리는 게 맞아서 `+ 새 앱` 은 전원에게 연다. 대신 **목록 전체의 모양을 바꾸는 것**
  (보관·주제 관리·주제로 옮기기)은 원장만 한다 — 한 사람이 누르면 모두의 화면이 바뀐다.
- 수정은 **원장 + `creator_id`(제작자)**. 배포 링크·마감일이 바뀐 걸 제일 먼저 아는 게
  만든 사람이고, 재검증 요청도 원래 같은 갈래였다. 갈래는 `/apps/[id]` 의 `canEdit` 한 줄이다.
  · 등록할 때 제작자를 **남으로 지정하면 등록한 본인은 못 고친다** — 이건 의도한 대로다.
    "제작자" 는 그 앱을 책임지는 사람이지 폼을 누른 사람이 아니다.
- **이 제한은 화면에만 있다.** DB 의 RLS(`internal_all`)는 `using (true) with check (true)`
  라 전 테이블이 열려 있다. 내부 5명이 쓰는 앱이라 지금은 이걸로 충분하다 —
  서버가 실제로 admin 을 확인하는 건 `/api/members` 하나뿐이다.
  권한을 진짜로 강제해야 할 일이 생기면 그때는 RLS 가 아니라 서버 라우트로 가야 한다
  (브라우저가 anon 키로 직접 붙기 때문).

---

## 파일 지도

```
supabase/schema.sql          moalab 스키마·테이블·RLS·Storage 버킷·초기 멤버 (한 번에 실행)
supabase/storage.sql         Storage 버킷 6개만 따로 — 버킷이 나중에 늘어났을 때 이것만 다시 돌린다
supabase/seed-apps.sql       노션에서 이관한 웹앱 21개 일괄 등록 (재실행 안전)
supabase/seed-org.sql        역할분장 예시 — 부서 5 · 중분류 15 · 소분류 40 (재실행 안전).
                             schema.sql 과 일부러 떼어놨다 — 거기 넣으면 지운 줄이 되살아난다

src/lib/
  types.ts                   DB 타입 + FINDING_META·REPLY_STATES(검증 상태) + 카테고리 상수
  supabase.ts                브라우저 클라이언트(schema: moalab) + friendlyError (한글 에러 변환)
  supabaseAdmin.ts           서버 전용 service_role 클라이언트(schema: moalab)
  session.tsx                로그인 세션 컨텍스트 (localStorage 30일)
  verify.ts                  라운드 생성 / 상태 재계산
  status.ts                  computeStatus, roundProgress, 상태 배지 스타일
  cost.ts                    원가 계산, 시트 합계, 구매 목록(장보기)
  revenueShare.ts            수익배분 — 직접비·성과풀·남은 1/N, 누진 영업/제안서 추천
  image.ts                   업로드 전 리사이즈 (긴 변 1600px, WebP)
  upload.ts                  리사이즈 → Storage 업로드 → 공개 URL
  zip.ts                     사진 zip 다운로드 (jszip 은 지연 로딩)
  log.ts                     활동 로그 기록 (실패해도 본 작업을 막지 않음)
  format.ts                  원화·날짜·D-day 표기
  useMembers.ts              멤버 목록 훅
  useTopics.ts               주제 목록 훅 (sort_order 순)
  useAppsOverview.ts         앱 요약(상태·진행률·미해결 댓글) — 목록 화면 공용
  push.ts                    usePush(구독 켜기·끄기) + sendPush(발송 요청)
  print.ts                   인쇄에 넣을 묶음 정의(PRINT_PARTS) + parts 파싱
  expense.ts                 지출 — 달 계산(shiftMonth·monthRange), 합계, 날짜별 묶기, 금액 입력
  task.ts                    업무 — 급한 순 정렬, 기한 묶기, 묶음 진행률, 사람별 부하·사람별 묶기,
                             날짜(shiftDate) + kstDateStr(서버의 '오늘' 은 한국 날짜가 아니다)
  taskParse.ts               말 → 업무 정리. AI 응답 검사(멤버 id·실제 날짜)+ 지시문 + 응답 스키마
  org.ts                     역할분장 — 트리 만들기, 미정 세기, 사람별 뒤집기, 검색, 순서 바꾸기
  ai.ts                      쓸 모델 목록·기본값·되돌리기(resolveModel). 관리 화면에서 원장이 고른다
  hwpx.ts                    강의계획서 → 한글 파일(.hwpx). OWPML XML 을 직접 짓고 jszip 으로 묶는다

src/app/(app)/
  home/ notice/ task/ apps/ verify/ mock/ training/ cost/ revenue/ gallery/ schedule/ admin/
                             메뉴 하나가 폴더 하나. 순서는 BottomNav.tsx 가 정한다.
src/app/api/cron/due/        기한 알림 — 매일 아침 오늘·내일 마감을 담당자별로 한 통씩
src/app/print/[id]/          프로그램 인쇄 / PDF 저장 (레이아웃 밖 — 사이드바·탭 없음)
src/app/print/expense/       월별 지출결의서 인쇄 (같은 이유로 레이아웃 밖)
src/app/print/lectures/      강의계획서 전체 인쇄 — 쓴 프로그램 전부, 한 장씩 (주제 순서)
src/app/print/status/        검증 현황 표 인쇄 — 검증 완료/미완료로 나눈 표
src/app/print/roles/         역할분장 인쇄 — 부서별 통합 / 사람별(한 사람=한 쪽) / 담당자 미정
scripts/make-icons.mjs       로고에서 앱 아이콘 뽑기
scripts/friendly-error.test.mjs  에러 문구 갈래 테스트 (node 로 바로 실행)
scripts/status.test.mjs      앱 상태 계산(수정 필요·다시확인·검증 완료) 표 검증
scripts/expense.test.mjs     지출 달 계산·합계·금액 입력 검증
scripts/hwpx.test.mjs        한글 파일(.hwpx) 생성 검증 — 한글이 거부하는 조건들을 미리 잡는다
scripts/task.test.mjs        업무 — 한국 날짜 경계(UTC 15:00), 월말·윤년, 급한 순 정렬, 묶음 진행률
scripts/task-parse.test.mjs  말 → 업무 — AI 응답을 안 믿는 검사(없는 사람·없는 날짜·폭주·빈 제목)
scripts/print.test.mjs       인쇄 통합 셀(rowSpan) — 표가 어긋나지 않는지, 떨어진 값을 안 합치는지
scripts/org.test.mjs         역할분장 — 주담당·부담당 겹침, 미정이 맨 위, 0건인 사람도 그리기, 순서
scripts/revenue-share.test.mjs 수익배분 — 1/N+성과몫, 누진 추천, 원 단위 잔여·적자·검증
scripts/measure.mjs          375px 에서 실제로 재기 — 세로 길이·가로 스크롤·44px 미만 탭 대상
scripts/measure-mock.mjs     가짜 데이터를 물려 375px 에서 재기 — 프로그램 페이지(/apps/[id])와
                             역할분장(/roles). Supabase 응답을 가로채므로 진짜 DB 없이 잰다
                             (dev 서버를 띄워두고 실행. playwright 는 이때만 필요해 package.json 에 없음)
scripts/measure-all.mjs      열두 화면을 **실제 데이터 규모 그대로** 그려놓고 재기.
                             REST 응답을 가로채 프로그램 33·주제 16·원가표 10·지적 14 로 물린다.
                             `첫 내용까지` 를 같이 재는 게 핵심 — 조작 줄이 먹는 높이가 간소화의 과녁이다

src/components/
  ui.tsx                     Sheet, ConfirmDialog, Skeleton, EmptyState, Toast 등
  BottomNav.tsx              메뉴 정의(WORK_NAV/RESOURCE_NAV) + 폰 하단 탭 · PC 사이드바
  PageHeader.tsx             상단 헤더
  AppForm.tsx                앱 추가/수정 — 저장 시 라운드·체크 자동 생성
  AppCard.tsx                앱 목록 카드 (리스트/보드/갤러리 3종 + PieceRow)
  PlanForm.tsx               강의계획서 작성 칸 (한글 양식 그대로 + 사진 넣기 + 로고)
  PlanSheet.tsx              강의계획서 인쇄 표 — 양식과 같은 표를 그린다 (인쇄 화면이 씀)
  LessonPlan.tsx             계획안 첨부 + 판(버전)
  CostInline.tsx             프로그램 페이지 안의 원가 요약
  SampleImages.tsx           프로그램 샘플 이미지
  Brand.tsx                  BrandMark(M 심볼) · BrandLogo(워드마크) · 멤버 아바타
  Icon.tsx                   인라인 SVG 아이콘 모음 (이모지 대체)
  MonthCalendar.tsx          월간 달력 그리드 (칸 안에 일정 제목까지 — 홈·일정 공용)
  Charts.tsx                 Sparkline · WeekBars · Timeline · StatCard (SVG 직접)
  TeamBoard.tsx              누가 뭘 했나/안 했나 — 사람 단위 협업 현황
  Findings.tsx               검증 전체 — 캡처+지적 쓰기 / 답변 스레드 / 검증 참여·완료 /
                             지난 라운드 기록(FindingHistory)
  PushToggle.tsx             푸시 알림 켜기·끄기 (아이폰 홈화면 추가 안내 포함)
  TaskTemplateManager.tsx    체크리스트 만들기 — 항목 + 기본 담당자 + 기준일 ±N일 (원장만)
  TaskSpread.tsx             체크리스트를 기준일에 얹어 통째로 뿌리기 (미리보기 필수)
  TaskDictate.tsx            말로 업무 넣기 — 큰 칸 + 폰 키보드 마이크, 쪼갠 뒤 미리보기 (원장만)
  TaskAssign.tsx             업무 체크해서 한 사람에게 한꺼번에 넘기기 (원장만, 요청 하나·알림 한 통)
  AiKeyCard.tsx              AI 키 등록·삭제 (관리 화면). 키는 화면으로 안 내려온다 — 끝 4자리만
  DutyForm.tsx               역할(소분류) 한 줄 — 이름·설명·주담당 1명·부담당 여럿
  TopicManager.tsx           주제 추가·이름변경·순서·삭제 (프로그램계획 > 주제 관리)
  TopicMove.tsx              프로그램 체크해서 주제로 한꺼번에 옮기기
  CommentThread.tsx          댓글 + 사진 첨부 + 해결됨 토글
  CostItemForm.tsx           원가 항목 입력 (사진·구매처·재사용)
  CostChart.tsx              구분별 도넛 + 막대 (SVG 직접 그림, 라이브러리 없음)
  (지출결의서는 화면 하나로 끝나서 별도 컴포넌트를 안 뺐다 — src/app/(app)/expense/page.tsx)
```

---

## 반드시 지킬 것 (수정할 때도)

- **모바일 우선.** 모든 화면을 폭 **375px** 에서 먼저 확인한다. 가로 스크롤이 나오면 실패다.
- 버튼은 최소 **44px** 높이 (`.tap` 클래스).
- 표를 모바일에서 그대로 쓰지 말고 **카드**로 바꾼다.
- 사진 업로드는 **반드시** `uploadFile()` 을 거친다 (리사이즈 후 저장).
- 로딩 중에 빈 화면을 두지 말고 **스켈레톤**을 보여준다.
- `useToast()` 는 `{show, node}` 를 준다. **`{toast.node}` 를 화면에 렌더하지 않으면
  `show()` 를 불러도 아무것도 안 뜬다.** 새 화면을 만들 때 이걸 네 곳에서 빼먹었다.
- 자동저장 금지. **명시적 저장 버튼**. (강사들이 잘못 눌렀다 날리면 안 된다)
- **다크 테마다.** `tailwind.config.ts` 에서 `neutral` 스케일을 통째로 뒤집어 놨다
  (50 = 가장 어두움, 900 = 가장 밝음). 그래서 화면 코드는 라이트 기준 그대로
  `text-neutral-900`(본문) / `bg-neutral-50`(옅은 배경) 처럼 쓰면 된다.
  카드 배경은 `bg-surface`, 한 단계 뜬 면은 `bg-raised`, 페이지는 `bg-canvas`.
  · 새 색을 쓸 땐 config 에 그 색의 50·100(어두운 틴트)과 700·800(밝은 글자)을 정의할 것
  · `bg-white` 를 새로 쓰지 말 것 — `bg-surface` 를 쓴다
- **PC 는 왼쪽 사이드바, 폰은 하단 탭.** 둘 다 `src/components/BottomNav.tsx`
  (`SideNav` / `BottomNav`). 분기는 `lg:` 브레이크포인트 하나뿐이다.
- **PC 에서 본문을 화면 끝까지 늘리지 않는다.** `(app)/layout.tsx` 가 `lg:max-w-[1280px]`
  로 자른다. 예전엔 `lg:max-w-none` 이라 27인치에서 버튼·진행바가 1500px 까지 늘어나
  *"막대기가 쩌끝까지 간다"* 가 됐다. 홈의 3단(`lg:grid-cols-3`)은 이 폭에서도 넉넉하다.
  · **단일 컬럼으로 읽는 화면은 더 좁게.** 프로그램 페이지(`/apps/[id]`)는 `lg:max-w-4xl`.
  · **역할분장(`/roles`)은 PC 에서 두 칸으로 흘린다**(`lg:columns-2`). 한 칸으로 두면
    역할 이름과 담당자 사이가 1200px 넘게 벌어져 눈이 못 따라간다. `grid` 가 아니라
    `columns` 인 이유는 펼친 부서 옆이 통째로 비어 덜 만든 화면처럼 보이기 때문이다.
    보기 토글은 `lg:max-w-lg`, 검색·미정 버튼은 PC 에서 한 줄로 붙인다
  · 가운데 정렬은 하지 않는다 — 헤더·사이드바와 **왼쪽 선을 맞춰야** 하기 때문이다.
- **이모지를 UI 에 쓰지 않는다.** 기기마다 모양·크기가 달라 화면이 지저분해진다.
  아이콘은 전부 `src/components/Icon.tsx` (인라인 SVG). 새 아이콘도 여기에 추가한다.
- **세로 스크롤을 아낀다.** 폰 한 화면이 812px 인 걸 기준으로 생각한다.
  · **필터 칩을 여러 줄로 늘어놓지 말 것.** 지출결의서에서 구분 7개 + 사람 5명을
    칩으로 깔았더니 네 줄(약 200px)을 먹어서 정작 지출 목록이 화면 밖으로 밀렸다.
    갤러리처럼 `<details>` 로 접고 머리글에 걸린 개수만 배지로 보여준다.
    한 줄에 들어가는 정도면 `/apps` 처럼 가로 스크롤 한 줄로 둔다.
  · **PC 오른쪽 묶음(팀 활동·주간 활동·프로그램 구성)은 폰에서 `hidden lg:block`.**
    폰에서는 그냥 아래로 483px 이 더 붙을 뿐이었다.
  · 작은 글씨 버튼은 `-my-3 py-3` 처럼 **패딩으로 넣고 마진으로 되돌려** 탭 면만 44px 로
    키운다 (줄 높이는 그대로라 화면이 길어지지 않는다).
  · 점검은 **`scripts/measure.mjs` 로 375px 에서 실제로 잰다** —
    가로 스크롤·세로 길이·44px 미만 탭 대상을 화면별로 뽑는다.
    `npm run dev` 를 띄워두고 `node scripts/measure.mjs`.
    · playwright 는 이때만 필요해서 `package.json` 에 안 넣었다 (`make-icons.mjs` 와 같은 처리):
      `npm i -D playwright` 후 실행하고, 이 환경처럼 크로미움이 미리 깔려 있으면 그걸 쓴다
    · **가짜 세션에 `expiresAt` 이 없으면 전부 로그인 화면으로 튕긴다** — 그러면 모든 화면이
      812px 로 나와 '전부 한 화면' 이라는 거짓 결과가 된다. 스크립트가 그걸 감지해 알려준다
    · 예전 문서가 가리키던 `scratchpad/audit.mjs` 는 **커밋된 적이 없다.** 그래서 이 문서의
      픽셀 숫자들은 지금까지 전부 확인 불가능한 서술이었다
  · 프로그램 페이지 섹션은 접이식 — 기본은 '검증'만 펼침
  · 검증 카드도 내 것만 펼침 (남의 건 통과 개수만)
  · **빈 섹션은 아예 그리지 않는다.** "없어요" 카드가 쌓이면 스크롤만 길어진다
- 폰트는 **에스코어드림**. `globals.css` 에서 굵기 4개(400·600·700·900)만 불러온다.
  한글 폰트는 굵기당 1MB 가까이 되니 굵기를 늘리지 말 것. `font-display: swap` 이라
  폰트를 받는 동안에도 시스템 폰트로 글자가 먼저 보인다.
- **파일을 올리는 저장은 두 단계로 나눈다.** 글을 먼저 저장하고 파일을 올린다.
  파일 올리기가 실패했을 때 다시 누르면 **글이 두 번 저장되던 버그**가 공지·지적 두 곳에 있었다.
  · 만들어진 행의 id 를 기억해서, 재시도는 새로 만들지 않고 그 행을 고친다
    (공지는 `setEditing`, 지적은 `createdRef`)
  · 올라간 파일은 목록에서 빼서 두 번 올라가지 않게 한다
  · 에러도 뭉치지 말고 **"글은 저장됐어요. 첨부만 못 올렸어요 — …"** 처럼 갈라서 알려준다
- 에러 메시지는 **한글**로, 개발자 용어 없이.
  `Failed to fetch` (X) → "인터넷 연결이 불안정해요. 잠시 후 다시 눌러주세요." (O)
  변환은 `friendlyError()` 에서 한다.
  · Storage(버킷 없음·형식 미지원)는 테이블 에러와 원인이 달라 따로 문구를 준다
  · **원인이 다른 것을 한 문구로 뭉치지 말 것.** `PGRST106`(스키마 미노출) /
    `PGRST205`(표 없음) / `PGRST204`(컬럼 없음)를 예전엔 하나로 묶어서,
    정작 `notice_files` 표가 없는 걸 "Exposed schemas 에 moalab 을 추가하세요" 로
    안내해 원장이 엉뚱한 곳을 봤다. 지금은 **없는 표·칸 이름을 그대로 짚어준다.**
    회귀를 막으려고 `scripts/friendly-error.test.mjs` 를 뒀다 (`node` 로 바로 실행)
  · **못 알아본 에러는 원문을 120자까지 괄호로 덧붙인다.** 폰에서는 콘솔을 못 보니
    "저장이 안 됐어요" 만 띄우면 무엇도 알 수 없다 — 캡처해서 물어볼 수 있어야 한다

---

## 진행 상황

### ✅ 1단계 — 뼈대와 검증
- [x] Next.js 14 + TypeScript + Tailwind 프로젝트 구성
- [x] Supabase 연결, `supabase/schema.sql` 전체 스키마 (테이블 15개 + RLS + Storage 3버킷)
- [x] PIN 로그인 (30일 세션, 서버 검증, PIN 노출 차단)
- [x] 앱 추가 폼 + 앱 목록 (검색·필터·상태 배지·진행률)
- [x] 검증 + 라운드 + 상태 자동 계산 (7단계에서 캡처·지적 방식으로 교체)
- [x] 홈 대시보드 (내 할 일, D-day, 진행률, 원장용 강사별 현황·지연·활동)

### ✅ 2단계 — 소통
- [x] 댓글 + 사진 첨부 (카메라롤 바로 열림) + 해결됨 토글
- [x] 일정 달력 (월간/주간, 마감 자동 표시, 회의·방문 직접 추가)

### ✅ 3단계 — 원가
- [x] 원가계산서 (재료 사진, 구매처·링크, 소모품/재사용 자동계산)
- [x] 하단 고정 요약 바 (인원 슬라이더, 1인당·총 원가, 판매가, 마진율 음수 빨강)
- [x] 구분별 소계 도넛/막대 그래프
- [x] 구매 목록 보기 (구매처별 묶음, 사진 포함, 장보기 리스트 복사)
- [x] 원가표 복제, 카톡 붙여넣기용 텍스트 복사

### ✅ 4단계 — 갤러리
- [x] 앨범 (학교 + 날짜 + 프로그램) + 여러 장 업로드 + 자동 리사이즈(1600px WebP)
- [x] 캡션·태그·얼굴 포함 체크·대표 사진 지정
- [x] 필터 (학교/프로그램/기간/태그/얼굴없는것만) + 선택 zip 다운로드
- [x] 앱 상세 → "이 프로그램 작품 보기"

### ✅ 5단계 — 마무리
- [x] 활동 로그 (원장 관리 화면에서만 조회, 검색 가능)
- [x] 카톡 복사, 진행률 그래프

### ✅ 6단계 — 업무 흐름대로 메뉴 재편
- [x] 왼쪽 메뉴를 **공지사항 / 프로그램계획 / 프로그램검증 / 모의수업 / 강사양성** 으로 재편
      (원가·갤러리·일정은 '자료' 묶음으로, 폰은 4탭 + 더보기)
- [x] 공지사항 — 고정 공지 + **읽음 표시** (누가 봤고 누가 아직인지)
- [x] 프로그램검증 — 내가 볼 것 / 내 답변 대기 / 전체. 새 테이블 없이 기존 데이터 재조합
- [x] 검증 **지적 → 답변** 한 바퀴 (7단계에서 캡처 중심으로 다시 만듦)
- [x] 모의수업 — 날짜·발표자 + "좋았던 점 / 고칠 점" 피드백
- [x] 강사양성 — 과정 × 강사 이수 상태 (강사는 내 과정, 원장은 전체 판)
- [x] PC 본문 첫 줄을 사이드바 로고와 같은 선에 맞추고, 홈 인사·요약을 한 줄로

### ✅ 7단계 — 검증을 캡처 + 지적 중심으로 교체
- [x] 고정 5항목 O/X 표를 걷어내고 **캡처 + 무엇이 이상한지** 로 바꿈 (`findings`)
- [x] 답변은 **수정완료 / 수정불가 / 다시확인 + 글(필수)**, 누구나, 스레드로 쌓임
- [x] 지적을 낸 사람이 **확인완료로 닫기**
- [x] 검증자의 **검증 완료** 표시(`round_signoffs`) — 지적 0건 + 전원 완료여야 `done`
- [x] (구) `checks`·`check_files` 는 기록 보존용으로 테이블만 남김

### ✅ 8단계 — 검증 참여 개방 · 공지 첨부 · 푸시 알림
- [x] 검증자는 누구나 — **자기 이름을 눌러 참여**, 남의 이름은 원장만
- [x] 공지에 **사진·파일 첨부** (사진은 바로 보이고, 문서는 확장자 라벨 + 내려받기)
- [x] 공지 **검색 + 안 읽은 것만** 필터, 홈에도 공지 카드(안 읽은 것 먼저 / 전체 N건 링크)
- [x] **푸시 알림** — 새 공지·새 지적·답변. 자기 행동은 자기한테 안 울림.
      죽은 구독 자동 정리. 아이폰 홈화면 추가 안내

### ✅ 9단계 — 모아킷 로고 적용
- [x] 원본 로고에서 투명 PNG 3종(심볼 / 마크 / 전체) 추출
- [x] PWA·아이폰·안드로이드·파비콘 아이콘 전부 생성 (검정 + teal M)
- [x] PC 사이드바는 moakit 마크, 로그인은 앱 아이콘과 같은 검은 판 위 심볼
- [x] `scripts/make-icons.mjs` — 로고 갈아끼우면 한 번에 다시 뽑힌다

### ✅ 10단계 — 주제별 트리 + 인쇄/PDF
- [x] `apps.topic` (자유 입력) + `/apps` **주제별 트리 보기**를 기본으로.
      접힘이 기본이라 폰 첫 화면에서 전체 구조가 보인다 (리스트 1862px → 트리 812px)
- [x] **주제 관리** — 한 곳에서 주제를 만들고 이름 고치고 순서 바꾸고 지운다.
      앱 폼은 드롭다운으로 고르기만 (폼 안 `+ 새 주제` 로 즉석 추가도 가능)
- [x] **주제로 옮기기** — 프로그램을 체크해서 한꺼번에 이동 (요청 하나로)
- [x] 토스트가 안 보이던 화면 4곳 수정 (`{toast.node}` 누락)
- [x] **인쇄 / PDF 저장** — 검증·계획안·원가·샘플·사진을 체크해서 A4 문서로.
      브라우저 인쇄를 쓰므로 라이브러리 없음

### ✅ 11단계 — 지출결의서 (영수증 증빙 → 달별 문서)
- [x] `moalab.expenses` + `expense_files` + `moalab-receipts` 버킷
- [x] 폰에서 **날짜·금액·구분·내용·영수증 사진** 만으로 한 건 등록 (나머지는 접어둠)
- [x] 달 넘기기 · 구분/사람/영수증없음 필터 · 구분별 소계 막대 · 원장 **확인**
- [x] **`/print/expense`** — 구분별·날짜별 소계 + 명세 표 + **번호가 일치하는 영수증 첨부**,
      서명란, 영수증 없는 건 빨간 칸으로 따로
- [x] 달 계산(연 경계·윤년)·합계·금액 입력 테스트 33건, 영수증 실패 시 재시도 중복 저장 방지 확인

### ✅ 12단계 — 검증 완료 한 번으로 끝내기 + PC 폭 정리
- [x] **수정완료된 지적은 '검증 완료' 를 누를 때 같이 닫힌다.**
      예전엔 `fixed` 가 남아 있으면 검증 완료 버튼이 아예 안 눌려서
      ("먼저 확인완료로 닫아주세요") 검증이 영영 안 끝났다
- [x] 막을 때는 몇 건이 남았는지 **버튼 안에 미리** 적어준다 (눌러보고 에러로 알게 하지 않는다)
- [x] `recomputeAppStatus` 가 빠진 검증자의 낡은 `round_signoffs` 를 세던 것 수정
- [x] `apps.status` 에 `check` 제약 — 지적 상태(`recheck`)가 흘러들어 인쇄 화면이 죽던 것 차단
- [x] **PC 본문 최대 폭** — `lg:max-w-none` 을 걷어내고 1280px, 프로그램 페이지는 896px
- [x] DB 에 안 올라가 있던 `plan_files`·`app_samples`·`apps.plan_body` 적용
      (스키마 파일엔 있었는데 실제 DB 에만 없어서 샘플 이미지·수업계획안 첨부가 죽어 있었다)

### ✅ 13단계 — 강의계획서를 한글 양식 그대로
- [x] 원장이 준 `강의계획서.hwp` 를 뜯어 칸 구성을 그대로 옮김
      (제목+분류 배지 · 과정/강의내용 · 목표 · 도입 · 전개 · 마무리 · 운영사항)
- [x] **작성 칸** `PlanForm` — 폰에서도 채울 수 있게 세로로. 저장은 명시적 버튼,
      단 사진·활동 줄은 고르는 즉시 저장 (올려놓고 날리면 더 나쁘다)
- [x] **전개 칸에 사진** — 웹앱활동은 가로 3칸, 만드는 순서·결과 이미지는 가로 2칸
- [x] **인쇄** `PlanSheet` — 양식과 같은 표를 그려 A4 한 장으로. 늘 새 쪽에서 시작.
      강의계획서만 고르면 표지 없이 그 한 장만 나간다 (`강의계획서 한 장만` 버튼)
- [x] **내용이 적어 한 장이 안 차면 전개 칸이 늘어나 A4 아래까지 채운다**
      (계획서 한 장만 뽑을 때. 표 높이를 100% 로 두고 전개 줄만 남는 높이를 가져간다)
- [x] **활동작품 항목이 없으면 그 블록 제목도 안 싣는다** — 빈 제목만 남으면
      덜 만든 문서처럼 보인다. [AI 웹앱활동]도 마찬가지
- [x] **맨 아래 로고는 올리는 칸** — 기관이 바뀌면 화면에서 갈아끼운다 (코드 수정 없음).
      로고가 없으면 `모아킷_교육을 위한 모든 것` 문구가 대신 실린다
- [x] 한글 원본 첨부(판) 기능은 `계획안 첨부파일` 로 그대로 남김
- [x] 카드의 `강의계획서` 배지는 **목표를 써서 저장했을 때** 켜진다 (`lesson_plans.goal`)

### ✅ 14단계 — 강의계획서를 A4 단면 한 장에 고정
- [x] **사진을 몇 장을 올리든 계획서가 두 쪽으로 안 넘어간다.** 예전엔 사진 칸이
      `aspect-[4/3]`(폭 기준 고정 비율)이라 사진이 쌓이는 만큼 세로가 무한정 길어졌다.
      이제 `PlanSheet` 가 장 전체를 인쇄 영역(186×268mm)으로 고정하고 사진 칸
      높이(`--ph`)를 "안 넘치는 가장 큰 값"으로 이진 탐색해서 잰다 — 사진이 많으면
      전부 같이 줄어들고, 내용이 적으면 커져서 아래까지 채운다 (`fill` 옵션 제거)
- [x] 사진은 자르지 않고(contain) 칸에 맞춰 줄어들기만 한다 — 구조(표·칸 병합)는 그대로
- [x] **화면 미리보기 = 인쇄 배치.** 장을 mm 단위 고정 폭으로 그리고 화면에서는
      폭에 맞게 통째로 축소한다(transform). 폰 375px 에서도 가로 스크롤 없음
- [x] 묶음 인쇄에서 계획서 뒤에도 쪽을 가른다(`print-page-break-after`) —
      뒤 섹션이 계획서 장의 남은 1mm 틈으로 파고들지 않게
- [x] 크로미움에서 사진 8장·사진 2장·글 초과 세 경우 모두 PDF 정확히 1쪽 확인
- [x] **활동작품(만드는 순서·결과 이미지)도 웹앱활동처럼 가로 한 줄로.**
      두 세로 열로 쌓았더니 사진 3장에 한 장이 꽉 차서 사진만 작아졌다.
      묶음마다 한 줄(최대 3칸, 넘치면 다음 줄), 사진이 1장뿐인 묶음은 반 폭으로 가운데

### ✅ 15단계 — 강의계획서를 한글 파일(.hwpx)로 받기
- [x] 학교·기관 제출용으로 **한글에서 바로 열리는 파일**을 만든다.
      `.hwp` 는 독점 바이너리라 불가능해서 국가표준 `.hwpx`(OWPML)로 간다
- [x] **새 의존성 0개** — jszip(이미 있음) + 직접 지은 XML (`src/lib/hwpx.ts`)
- [x] 표 8줄(제목·과정·목표·도입·전개·마무리·운영사항 2줄), 칸 병합,
      사진은 인쇄물과 같은 가로 배치(최대 3칸, 1장이면 가운데)
- [x] `header.xml` 의 `binDataList` 누락으로 **사진만 사라지던 것**을 되읽기 검사로 잡아 수정
- [x] 검증: 실제 브라우저에서 받은 파일을 뜯어 `mimetype`(첫 항목·STORE·application/hwp+zip),
      패키지·문서 검사 **경고 0건**, 그림 7장·참조 7건, 표 8줄, 내용·이스케이프·여러 줄까지 확인
- [x] `scripts/hwpx.test.mjs` 37건 — 한글이 거부하는 조건을 회귀로 막는다
- [x] **사진이 깨진 아이콘으로 나오던 것 수정** — 앱이 사진을 WebP 로 저장하는데
      한글은 WebP 를 못 읽는다. 캔버스로 다시 그려 JPEG(사진)·PNG(로고)로 바꿔 넣는다.
      덤으로 긴 변 1200px 로 줄어 파일이 926KB → 55KB
- [x] 맨 아래 **로고도 그림으로** 들어간다 (전에는 로고를 올려도 안 실렸다)
- [x] 결과 이미지처럼 **1장뿐인 묶음을 가운데로** — 원본 양식과 같게

### ✅ 16단계 — 프로그램 등록을 강사에게도 열기
- [x] **`+ 새 앱` 을 전원에게.** 예전엔 `isAdmin` 일 때만 버튼을 그려서
      강사 폰에는 **버튼이 아예 안 보였다** — 못 누르는 게 아니라 그런 게 있는 줄도 몰랐다.
      만든 사람이 그 자리에서 올려야 등록이 안 밀린다
- [x] **제작자는 자기 프로그램을 고칠 수 있다** (`canEdit = isAdmin || creator_id === 나`).
      등록만 열고 수정을 원장에게 두면 오타 하나·바뀐 배포 링크 하나에도 원장을 거쳐야 한다.
      **재검증 요청이 이미 쓰던 갈래**라 이름만 `canReopen` → `canEdit` 로 합쳤다
- [x] **보관 · 주제 관리 · 주제로 옮기기는 원장 그대로** — 한 사람이 누르면
      모두의 목록 모양이 바뀌는 것들이라 등록·수정과 성격이 다르다
- [x] 프로그램이 0개일 때 안내문도 전원에게 `“+ 새 앱” 버튼으로…` 로 통일
      (강사에게 "등록되면 여기 보여요" 라고 하면 여전히 못 올리는 줄 안다)
- [x] 권한 표를 이 문서 **로그인 / 권한** 에 정리 — 화면에만 있는 제한이고
      DB(RLS)는 열려 있다는 사실까지 같이 적었다

### ✅ 17단계 — 업무 분장 (1단계: 그릇 만들기)

> **왜 만들었나** — 강사 4명에게 "강사 역할을 하세요" 라고 큰 방향만 줬더니
> 스스로 할 일을 찾아 하지 못했다. 그런데 앱에는 **할 일을 담을 그릇이 아예 없었다.**
> `내 할 일` 은 프로그램 검증에서만 파생돼서(*내가 검증자인데 안 누른 것*, *답 안 한 지적*),
> "8/25까지 A초 제안서 — 이서은" 같은 걸 넣을 데가 없었다.

- [x] `moalab.tasks` + `task_templates` + `task_template_items` (schema.sql 17번 섹션)
      · **기존 표의 칸은 하나도 안 건드렸다** — 순수 추가라 되돌려도 표만 남는다
      · `reminded_on` — 기한 알림이 하루에 두 번 울리는 걸 칸 하나로 막는다
      · `batch_id`/`batch_title` 은 **비정규화**. 묶음은 일회성이라 `topics` 처럼 표로 뺄 이유가 없다
- [x] `/task` 화면 — 급한 순 목록(지남·오늘·이번 주·나중), 3단 상태 토글, 끝낸 것은 접힘
      · 강사는 `내 업무`, 원장은 `내 업무 / 전체` + **사람별 남은 업무** 카드
      · 담당자 미정 건수를 원장에게 따로 짚어준다 — 그게 제일 먼저 채워야 할 것이다
- [x] **업무 등록은 전원에게.** 강사가 만들면 담당자 기본값이 자기 자신이다
      (원장은 나눠주는 쪽이라 비워둔다)
- [x] **배정하면 그 사람에게만 알림** — `sendPush({ memberIds })` 를 **처음 실제로 쓴 자리**다.
      지금까지 알림은 서버가 대상 좁히기를 지원하는데도 전부 전원에게 갔다
- [x] `scripts/task.test.mjs` 40건 — 한국 날짜 경계(UTC 15:00), 월말·연말·윤년,
      완료된 업무를 '기한 지남' 으로 세지 않기, 급한 순 정렬, 묶음 진행률
- [x] 폰 하단 탭은 **4개 그대로**. 업무는 더보기에 넣고, 대신 홈에서 다 보이게 할 예정(3단계)

### ✅ 18단계 — 업무 분장 (2단계: 체크리스트로 한 번에 뿌리기)

> 나눠주는 일은 매번 비슷하다. 매번 8줄을 타이핑하게 두면 결국 안 쓴다.

- [x] `TaskTemplateManager` — 체크리스트 한 벌을 만들고 항목마다
      **제목 + 기본 담당자 + 기준일 대비 며칠**을 적어둔다 (원장만)
      · 음수(-5 = 5일 전)가 헷갈리지 않게 **"오늘이 기준일이라면 8월 16일"** 을 화면에 미리 보여준다
- [x] `TaskSpread` — 템플릿과 기준일만 고르면 **미리보기 표**가 뜬다.
      담당자는 줄마다 바꿀 수 있고, 저장은 **insert 한 번**(배열)이라 반만 들어가는 일이 없다
      · 묶음 이름은 `${템플릿명} — ${기준일}` 로 자동, 손대면 그때부터 사람 것이다
      · **알림 통수를 미리 적어준다** — "사람당 한 통씩, 모두 3통 갑니다 / 이서은 2건 · 주은서 1건"
      · 담당자 미정이 몇 건인지도 미리 짚어준다 (이대로 만들어도 되게)
- [x] **묶음 통째로 삭제 / 묶음 전체 N일 미루기** — 한 번에 뿌린 것은 한 번에 물릴 수 있어야 한다.
      미룰 때 끝난 업무와 기한 없는 업무는 안 건드린다
- [x] `tasks` 에 `template_id` 를 **두지 않았다** — 뿌린 업무는 그 순간 사진처럼 굳는다
- [x] `scripts/task.test.mjs` 63건으로 확장 — 기준일 ±N일(월초·윤년·연 경계),
      미리보기에서 바꾼 담당자가 기본값을 이기는지, **빈 값은 '일부러 비운 것'** 으로 남는지,
      알림이 사람 수만큼만 나가는지, 묶음 미루기가 완료·기한없음을 건너뛰는지

### ✅ 19단계 — 말로 던지면 업무로 (원장 전용)

> 원장이 폰에 대고 *"이서은은 다음 주 화요일까지 A초 제안서 쓰고, 주은서는 재료 주문해줘"*
> 라고 말하면 업무 두 건으로 쪼개진다. 이 기능은 **원장 한 사람만** 쓴다.

- [x] `moalab.app_secrets` — API 키 표. **PIN(`members`)과 똑같이 잠갔다.**
      RLS 켜고 정책 없음 + `revoke ... from anon, authenticated` + service_role 만.
      `internal_all` 배열에 넣으면 키가 통째로 새어 나간다 (문서에 경고를 박아뒀다)
- [x] `AiKeyCard` (관리 화면) — 등록·바꾸기·삭제. **키는 화면으로 다시 안 내려온다**,
      끝 4자리만 보여준다. 이미 등록돼 있으면 한 줄로 접힌다 (`PushToggle` 과 같은 판단)
- [x] `/api/settings/ai` · `/api/task/parse` — 둘 다 `x-actor-id` 로 원장인지 확인.
      오늘 날짜와 멤버 목록은 **서버에서** 넣는다
- [x] `TaskDictate` — 큰 칸 + 폰 키보드 마이크. 쪼갠 결과는 **저장하지 않고 미리보기**로만,
      제목·담당자·기한을 줄마다 고치고 뺄 수 있다. 알림은 사람당 한 통
- [x] **AI 응답을 안 믿는 검사층** (`taskParse.ts`) — 없는 사람은 담당자 미정으로,
      없는 날짜(2/30·평년 2/29)는 기한 없음으로, 빈 제목은 버리고, 20건에서 자른다
- [x] `scripts/task-parse.test.mjs` 46건 — 위 검사를 전부 회귀로 막는다
- [x] **새 의존성 1개** (`@anthropic-ai/sdk`). 지금까지 0개였는데 여기서 처음 늘었다 —
      API 클라이언트는 직접 짤 대상이 아니다

### ✅ 20단계 — 홈을 '오늘 할 일' 한 화면으로

> 폰 홈에 접히는 섹션이 **하나도 없어서** 여섯 덩어리가 전부 펼쳐진 채 3~4화면이었다.
> `order-*` 로 순서만 바꿔놓고 다 고친 줄 알았는데, 순서는 길이를 1px 도 안 줄인다.

- [x] `ui.tsx` 에 **`Collapsible`** — 같은 '접기' 가 세 군데에서 다른 방식으로 구현돼 있던 것을
      하나로. 펼침 상태를 기기에 기억하고(`moalab.open.*`), 접힌 채로도 급한지 보이게 배지를 남기고,
      닫히면 자식을 아예 안 그린다
- [x] 홈 6섹션(달력·전체 현황·마감 타임라인·공지·내 할 일·팀 현황)을 전부 접이식으로.
      **기본 펼침은 `내 할 일` 하나뿐**이다 — 나머지는 히어로 칩과 머리글 배지로 충분하다
- [x] **`myTasks` 에 업무 합류.** 카드 링크가 `/apps/${id}` 하드코딩이라 줄마다 `href` 를 들고
      다니게 고쳤다 (프로그램이 아닌 할 일을 얹을 수 없던 진짜 이유)
      · 6건 넘으면 조용히 자르지 않고 `N건 더 보기` 를 그린다
- [x] **마감 타임라인 `order-3` 하드코딩 수정** — 강사 화면에서 원장용 정보가 3번째로 끼어들었다
- [x] 팀 현황은 자르는 코드가 없어 사람 수만큼 길어졌는데, 접어두는 것으로 해결했다
- [x] **`scripts/measure.mjs` — 375px 에서 실제로 재는 도구를 처음 만들었다.**
      문서가 가리키던 `scratchpad/audit.mjs` 는 커밋된 적이 없어서, 지금까지 이 문서의
      픽셀 숫자는 전부 확인 불가능했다
      · 재보니 **접이식 머리글이 44px 미만**이라 규칙 위반이었다 — 바로 고쳤다 (7곳 → 0곳)
      · 가짜 세션에 `expiresAt` 을 안 넣으면 전부 로그인 화면으로 튕겨서 '전부 812px' 이라는
        거짓 결과가 나온다. 실제로 한 번 속았고, 스크립트가 그걸 감지하게 했다

### ✅ 21단계 — 기한 알림 자동 + 알림 결함 세 가지

> 지금까지 알림은 전부 **"그 순간 한 번"** 이 전부였다. 그 한 번을 놓치면
> 앱에 들어올 계기가 영영 없어진다. 기한이 닥치면 담당자에게 다시 울린다.

- [x] `vercel.json` + `/api/cron/due` — **UTC 23:00 = 한국 아침 8시**, 하루 한 번.
      오늘·내일 마감을 담당자별로 묶어 **그 사람에게만** 한 통씩
      · Vercel 무료 플랜은 cron 이 하루 1회라 저녁/아침 두 번을 못 쓴다 →
        아침 한 통에 `오늘 2건 · 내일 1건 — A초 제안서 외 2건` 을 같이 담는다
      · `CRON_SECRET` 이 없으면 **401 로 막는다** (열어두지 않는다). 실제로 세 경우를
        확인했다: 키 없음 401 / 틀린 키 401 / 맞는 키 200
- [x] **`tasks.reminded_on`** — cron 이 두 번 돌아도 한 통이다.
      알림을 안 켠 사람 몫도 '보낸 것' 으로 표시한다 (안 그러면 매일 같은 줄을 다시 시도한다)
- [x] 날짜는 전부 **`kstDateStr()`** — 서버는 UTC 라 그냥 짜면 알림이 하루 밀린다.
      경계(UTC 15:00)를 테스트로 못박아 뒀다
- [x] **같이 고친 알림 결함 셋**
      · `sw.js` 가 **없는 파일**(`/icon.svg`)을 알림 아이콘으로 가리켰다 → `/icon-192.png`.
        안드로이드에서 브랜드 아이콘 대신 크롬 기본 아이콘이 뜨고 있었다
      · `notificationclick` 의 `client.navigate()` 가 **이 서비스워커가 제어하지 않는 창에서
        실패**하면 포커스만 되고 알림이 가리키는 화면으로 안 갔다 → 실패하면 새 창을 연다
      · 공지 저장 시 **실제 발송과 무관하게** "알림도 보냈어요" 가 떴다 (키가 없어도,
        구독자가 0명이어도). `sendPush()` 가 결과를 돌려주게 하고 **정말 몇 명에게 갔는지**
        적는다 — `3명에게 알림도 갔어요` / `알림 받는 사람이 아직 없어요`
- [x] `scripts/task.test.mjs` 75건 — 완료·담당자없음·기한지남·오늘 이미 보냄을 걸러내는지,
      잠금화면 문구가 오늘을 앞세우는지

### ✅ 22단계 — 실제로 띄워놓고 본 UX 점검 + AI 모델 고르기

> 가짜 데이터(멤버 5·프로그램 5·업무 11·공지 2)를 Supabase REST 응답으로 물려
> **실제처럼 그려놓고** 봤다. 짐작이 아니라 실측이라 그동안 안 보이던 게 나왔다.

- [x] **강사 홈에 원장용 정보가 섞여 있던 것** — 지난번 홈 개편에서 순서만 바꾸고
      *누구에게 보일지*는 안 나눴다. 강사 히어로에 `팀 미착수`·`검증 완료 %` 가 뜨고
      `팀 현황 — 누가 뭘 하고 있나` 섹션이 통째로 보였다.
      · 칩을 역할별로 갈랐다 — 원장 `내 할 일·팀 미착수·7일 내 마감` /
        강사 `내 할 일·기한 지남·오늘`. **셋씩만** 둔다 (4개는 375px 에서 두 줄로 접힌다)
      · 팀 현황은 **원장만**. 강사가 남의 미착수를 봐도 할 수 있는 게 없고 감시처럼 느껴진다
      · 히어로 숫자와 섹션 배지가 서로 다른 걸 세고 있었다 → 같은 기준으로
- [x] **홈에서 할 일을 못 끝내던 것** — "강사는 홈만 보면 된다" 고 만들어놓고
      정작 가장 흔한 동작인 완료 표시를 홈에서 못 했다 (카드가 링크라 이동만).
      왼쪽 **동그라미를 누르면 그 자리에서 완료**된다. 한 번 더 누르면 되돌아가므로
      되돌리기 버튼이 따로 필요 없다
- [x] **업무 카드 163px → 129px** — `고치기·삭제` 줄을 카드마다 깔던 것을 제목 옆 `⋯` 하나로
      (삭제는 수정 시트 안으로). 끝난 업무는 3버튼 대신 `되돌리기` 하나만
- [x] **상태 버튼 36px → 44px** — 규칙 위반이었다. 셋이 나란히 있어서 잘못 누르기 쉽고
      잘못 누르면 바로 저장된다
- [x] **업무 화면 아이콘 버튼이 뭔지 모르던 것** — 확성기·≡ 를 헤더에서 내리고
      `말로 넣기` · `체크리스트로 뿌리기` · `체크리스트 관리` 라벨 줄로. 부제 잘림도 사라졌다
- [x] **프로그램 목록 컨트롤 359px → 299px** — `빠진 것` 칩 줄을 접고 정렬과 한 줄로 합쳤다
      (`빠진 것` 필터가 걸려 있으면 저절로 펴진다)
- [x] **44px 미만 탭 대상: 열 화면 전부 0건** (점검 전 원장 홈 1 · 업무 6 · 프로그램 3)
- [x] **AI 모델 고르기** (`src/lib/ai.ts` + `관리 > AI 키`) — 말 쪼개는 일은 어려운 축이 아니라
      가장 싼 것으로도 충분하다. 세 가지 중에 고르고 **한 번에 대략 얼마인지** 화면에 적었다.
      키를 다시 넣지 않고 모델만 바꿀 수 있다 (화면에 안 보이는 값을 또 찾아오게 하면 안 된다)

| | 점검 전 | 점검 후 |
|---|---|---|
| 홈 (원장 / 강사) | 1,181px | **1,065 / 1,017px** |
| 업무 카드 한 장 | 163px | **129px** |
| 프로그램 첫 카드까지 | 359px | **299px** |
| 44px 미만 탭 | 10곳 | **0곳** |

### ✅ 23단계 — 업무를 '나눠주는 화면' 으로

> 말로 넣기로 5건이 들어왔는데 **전부 담당자 미정**이었다. 그걸 채우려면 카드마다
> `⋯ → 고치기 → 담당자 → 저장` 을 스무 번 눌러야 했다. 그리고 목록은 급한 순
> 한 줄뿐이라 **누가 뭘 맡았는지가 어디에도 안 보였다.** 나누는 게 이 화면의 목적인데
> 나눌 수도, 나눈 결과를 볼 수도 없었다.

- [x] **나눠주기** (`TaskAssign`) — 체크해서 한 사람에게 한꺼번에. `주제로 옮기기` 와
      같은 방식이라 저장은 **요청 하나**, 알림은 **사람당 한 통**이다
      · **고르기 전에는 버튼이 안 눌린다.** 목록 맨 위가 원장 자신이라 그냥 누르면
        5건이 전부 자기한테 오고 알림까지 나간다 — 여러 건이 한 번에 움직이는 동작이라
        되돌리는 것도 여러 번이다. 실제로 눌러보고(playwright) 막히는 걸 확인했다
      · 들어가는 자리 셋: `나눠주기` 칩(미정 건수 배지) · 부하 카드의 미정 줄 ·
        사람별 보기의 `담당자 미정` 머리글
- [x] **사람별 보기** (`groupByPerson`) — `급한 순 / 사람별` 토글. 담당자 미정이 맨 위,
      업무 0건인 사람은 안 그린다. PC 는 두 칸으로 깔아서 다섯 사람이 한 화면에 들어온다
- [x] **원장은 `전체` 로 시작한다** — 원장 '내 업무' 는 비어 있는 날이 많은데, 거기서
      시작하면 미정 5건이 있어도 **빈 화면**을 본다 (실제로 그랬다)
- [x] **PC 카드 137px → 71px** — 상태 3버튼을 아래로 깔면 버튼 하나가 400px 까지 늘어나
      회색 벽이 된다. `lg:` 에서 제목 오른쪽으로 붙였다 (폰은 그대로 129px)
- [x] `사람별 남은 업무` 카드는 사람별 보기에서 **안 그린다** — 바로 아래에 같은 것이
      펼쳐져 있어 같은 숫자를 두 번 읽게 하고 300px 을 먹었다
- [x] **44px 위반 2곳 수정** — 새로 만든 묶음 선택 머리글, 그리고 그 패턴을 베껴온
      `TopicMove` 도 원래 같은 위반이었다 (측정에서 같이 잡혔다)
- [x] `scripts/task.test.mjs` 88건 — 미정이 맨 위인지, 0건인 사람을 안 그리는지,
      **지워진 멤버의 업무가 화면에서 사라지지 않는지**

| | 점검 전 | 점검 후 |
|---|---|---|
| PC 업무 카드 한 장 | 137px | **71px** |
| PC 사람별 전체 세로 | — | **900px (한 화면)** |
| 담당자 5건 채우기 | 스무 번 누름 | **체크 5 + 사람 1 + 넘기기** |
| 44px 미만 탭 | 2곳 | **0곳** |

### ✅ 24단계 — 웹앱마다 강사교육 문서 올리기 (갈래)

> 원장이 **웹앱별 강사교육안과 양식을 문서로 올려두고** 싶어 했다.
> 처음엔 `강사관리` 안에 새 메뉴를 만드는 것으로 계획했는데, 뜯어보니
> **올릴 자리가 이미 있었다** — `계획안 첨부파일` 이 판 쌓기·미리보기·zip 받기까지
> 갖춘 문서함이고, 안내 문구도 *"지도안 · 활동지 · PPT 를 올려주세요"* 였다.
> 그래서 **새 메뉴도, 새 표도, 새 버킷도 만들지 않았다.**

- [x] `plan_files.kind` **칸 하나** — `계획안`(기본값) · `교육안` · `양식` · `기타`.
      형식이 아니라 **누가 읽는가**로 가른다 (`src/lib/types.ts` 의 `PLAN_KINDS`)
- [x] **올릴 때 갈래를 고르는 시트** — 고르기 전에는 버튼이 안 눌린다.
      미리 골라두면 여러 개가 한꺼번에 엉뚱한 갈래로 들어간다 (`TaskAssign` 과 같은 판단)
- [x] **있는 갈래만 칩으로**, 갈래가 하나뿐이면 줄 자체를 안 그린다.
      `최신 전체 받기` 는 화면에 보이는 것만 묶고 파일 이름도 갈래를 따라간다
- [x] **말 정리** — 같은 것을 세 이름으로 부르고 있었다:
      화면은 `계획안 첨부파일`, 코드·활동로그는 `수업계획안`, 안내 문구는 `지도안`.
      섹션 이름을 **`문서 첨부`** 로 넓히고(이제 계획안만 담지 않는다) 나머지를 맞췄다.
      · 안내 문구에서 **'지도안' 을 뺐다** — 그 말이 남아 있으면 강사가
        교육안을 어디에 올릴지 헷갈린다
      · 카드 배지(`PIECES`)도 `계획안` → `문서` 로. 뜻이 이미 '문서가 하나라도 있음' 이었다
- [x] **`scripts/measure-mock.mjs`** — 프로그램 페이지를 375px 에서 재는 도구를 새로 만들었다.
      `measure.mjs` 는 목록 다섯 화면만 재고 진짜 데이터가 있어야 하는데,
      **제일 긴 화면은 상세**다. Supabase 응답을 가로채 가짜 데이터를 물리므로
      DB 도 `.env.local` 도 없이 잰다
- [x] 재보고 고친 것 — 갈래 칩을 넣자 **카드 버튼이 40px** 로 내려갔고(44px 규칙 위반),
      `고쳐서 올리기`·`지난 판` 은 원래 **19px** 이었다. 셋 다 `-my-3` + `min-h-[44px]` 로
      되돌렸다 (줄 높이는 그대로라 화면이 안 길어진다)
- [x] 갈래 칩을 파일 이름과 **한 줄에** 붙였다 — 따로 한 줄 잡으면 문서 한 장마다 24px 이다
      (문서 5개 기준 2402px → **2292px**)

| | 넣기 전 | 넣은 뒤 |
|---|---|---|
| 문서 첨부 펼침 (문서 5개) | — | **2292px** (한 줄 배치 전 2402px) |
| LessonPlan 안 44px 미만 탭 | 5곳 (`고쳐서 올리기` 19px) | **0곳** |
| 새 표 · 새 버킷 · 새 메뉴 | — | **0개** |

> ⚠️ **`/apps/[id]` 에 44px 미만 탭이 10곳 남아 있다** — 목차 칩 7개(32px),
> 배포 링크(16px), 검증자 칩 2개(34px). 전부 이번 작업 전부터 있던 것이고
> `LessonPlan` 밖(`apps/[id]/page.tsx`)이라 손대지 않았다. 지금까지 안 보였던 이유는
> `measure.mjs` 가 **상세 화면을 아예 안 쟀기** 때문이다. 목차 칩은 높이를 바꾸면
> 따라붙는 줄 배치가 같이 움직여서 따로 한 번 재면서 고쳐야 한다.

### ✅ 25단계 — 역할분장 (부서 › 중분류 › 소분류 › 담당자)

> 강사가 스스로 할 일을 못 찾던 것을 17단계에서 `업무` 로 풀었는데, 그건 **1건짜리**다.
> *"SNS 는 원래 누구 담당인가"* 처럼 **기한 없이 계속 남는 책임**을 담을 데가 없었다.

- [x] 표 넷 — `departments` · `duty_groups` · `duties` · `duty_helpers` (schema.sql 19번)
      · 기존 표의 칸을 하나도 안 건드렸다. 순수 추가라 되돌려도 표만 남는다
      · 주담당은 `set null`(역할은 남고 미정으로), 부담당은 `cascade`
- [x] `/roles` 화면 — `부서별 / 사람별 / 내 역할` 세 보기.
      원장은 `부서별`, 강사는 `내 역할` 로 시작한다
- [x] **담당자 미정이 맨 위**, `담당자 미정 N건만 보기` 버튼, 부서·중분류 머리글에 미정 배지
- [x] **역할 0건인 사람도 그린다** — 업무와 반대다. 아무것도 안 맡은 사람이 있다는 것
      자체가 봐야 할 정보다
- [x] 주담당으로 고르면 **부담당에서 자동으로 빠진다** (안 그러면 한 사람이 두 번 세어진다)
- [x] `supabase/seed-org.sql` — **이 회사가 실제로 하는 일**에서 뽑은 예시
      (기획 → 웹앱 제작 → 검증 → 강의계획서 → 모의수업 → 출강 → 정산).
      부서 5 · 중분류 15 · 소분류 40, **담당자는 일부러 비워뒀다** —
      임의로 찍으면 합의되지 않은 분장을 기록하는 셈이다
- [x] `scripts/org.test.mjs` 40건 — 겹친 담당, 미정이 맨 위, 0건인 사람, 순서 바꾸기,
      `sort_order` 가 전부 0 인 옛 데이터, 부모 없는 줄
- [x] `scripts/measure-mock.mjs` 를 넓혀 `/roles` 도 375px 에서 잰다

| 폰 375px (부서 5 · 역할 45 · 미정 15) | |
|---|---|
| 부서별 전부 접힘 | **812px — 정확히 한 화면** |
| 부서 하나 펼침 | **1031px** (중분류를 접기 전 1499px) |
| 담당자 미정만 보기 | **1164px** (트리로 그리던 2949px → 평평한 목록) |
| 사람별 | 812px (미정을 펼쳐두면 1203px 이라 접힘 기본 + 빨간 배지로 바꿨다) |
| 내 역할 | 1051px |
| 가로 스크롤 · 44px 미만 탭 | **없음 · 0곳** |

| PC 1440px | |
|---|---|
| 부서별 (하나 펼친 상태) | **900px** — 다섯 부서가 한 화면. 두 칸으로 흘린다 |

- [x] **트리를 두 단계 다 접이식으로.** 부서만 접으면 부서 하나를 여는 순간
      소분류가 전부 쏟아졌다 (1499px). `Collapsible` 에 `dense` 를 더해 중분류 머리글을
      한 단계 작게 그리고, 왼쪽 세로선으로 층을 표시했다
- [x] **`Collapsible` 의 '검색하면 저절로 펼친다' 가 실제로는 안 되고 있었다.**
      `useState(defaultOpen)` 이 첫 값만 잡아서 나중에 검색어가 들어와도 안 열렸고,
      기억해둔 상태가 있으면 그것이 이겼다. `forceOpen` 을 더해 고치고
      `measure-mock.mjs` 가 회귀로 확인한다 (검색 전 안 보임 → 검색 후 보임)
- [x] **역할 추가·수정·담당자 지정을 전원에게** 열었다. 원장만 두면 등록이 밀린다
      (`+ 새 앱` 과 같은 판단). 지우기와 부서·중분류는 원장 그대로
- [x] **PC 폭 정리** — 한 칸으로 두니 역할 이름과 담당자 사이가 1200px 벌어졌다.
      두 칸으로 흘리고(`lg:columns-2`) 토글·검색 줄 폭도 잡았다 (1164px → 900px)

### ✅ 26단계 — 프로그램별 수익배분 계산

- [x] `/revenue` 목록 → `/revenue/[appId]` 계산기. 프로그램 상세의 원가 바로 아래에도 연결
- [x] 실제 수금액에서 직접비를 먼저 빼고 창작·사업계획서·영업 성과몫을 계산한 뒤 남은 금액 1/N
- [x] 성과 담당자가 두 명이면 그 풀만 1/2, 담당자도 기본 1/N을 다시 받는 구조
- [x] 영업·제안서 규모별 누진 체감 추천 + 직접 입력 전환. 창작 기본 15%
- [x] 민간매출·공공 용역계약·보조지원금 재원 구분과 보조금 경고
- [x] 원가표의 예상 매출·직접비 불러오기, 사람별 계산 근거, 원장 명시적 저장
- [x] 정수 원 단위 합계·입력 순서 불변·적자·초과율·구간 경계 회귀 테스트

### ✅ 27단계 — 월별 수익배분 계산안

- [x] `/revenue` 달 이동 → 달별 수금액·직접비·배분가능이익과 사람별 프로그램 합산 예상액
- [x] `/revenue/[appId]`에서 프로그램×월 계산안을 독립 저장. 같은 프로그램의 다른 달을 덮어쓰지 않음
- [x] 새 달 금액은 0원부터 시작하고, 지난달 참여자·비율은 사용자가 눌렀을 때만 가안으로 복사
- [x] 비율이 미정이어도 월 실적을 저장할 수 있도록 `비율 미정 / 검토안 / 합의됨` 상태 분리
- [x] 월 계산 결과와 당시 멤버 이름 스냅샷 저장, 여러 프로그램 사람별 합산 회귀 테스트
- [x] 운영 DB에 별도로 붙여넣을 수 있는 `supabase/revenue-share.sql` 추가

### 다음에 하면 좋을 것

- [ ] **모의수업·강사양성은 뼈대만 만든 상태다.** 실제로 어떤 칸이 필요한지
      (모의수업 참석자 체크? 강사양성 과정 목록?) 원장 확인 후 채운다.
- [ ] 사진 Storage 실제 파일 삭제 — 지금은 DB 레코드만 지운다 (버킷에 파일이 남음).
      지적 캡처(`finding_files`)·영수증(`expense_files`)도 같은 문제라 정리 스크립트가 하나 필요하다.
- [ ] 댓글에 답글(스레드) — 지금은 평면 목록
- [ ] UI 강조색을 로고 teal(`#06BDBD`)로 맞출지 — 지금은 오렌지 그대로다 (원장 취향 확인 필요)
- [ ] `/apps/[id]` 의 44px 미만 탭 10곳 (24단계 경고 참고) — 목차 칩·배포 링크·검증자 칩
- [ ] 홈에 **내 역할** 한 줄 — 지금은 `/roles` 에 들어가야만 보인다
- [ ] 업무(`tasks`)에 역할을 걸어 *"이 역할에서 나온 일"* 로 묶기 — 지금은 서로 모른다
- [ ] 강사가 *"이번에 나갈 자료 어디"* 를 자주 헤매면 **교육안 모아보기 화면**.
      `plan_files.kind` 가 이미 있어서 새 표 없이 화면만 얹으면 된다
      (계획은 `docs/training-guide-naming.md`)

---

## 작업 방식

- 이 문서에 단계 완료 시 무엇을 만들었는지, 다음에 뭘 할지 기록한다.
- 환경변수는 `.env.local`, 공유용 템플릿은 `.env.example`.
- 스키마 변경은 반드시 `supabase/schema.sql` 에도 반영한다 (여러 번 실행해도 안전하게).
- **⭐ DB 를 건드렸으면 돌려야 할 SQL 을 채팅에 그대로 붙여준다.**
  원장이 Supabase SQL Editor 에 **복사해서 붙여넣기만 하면 되게** 한다.
  · *"schema.sql 을 다시 실행하세요"* 로 끝내지 않는다 — 파일을 찾아 열고 어디를
    복사할지 고르는 일이 매번 생긴다. 그게 밀리면 화면은 배포됐는데 표가 없어서
    "저장이 안 돼요" 가 된다 (실제로 12단계에서 `plan_files`·`app_samples` 가 그랬다)
  · **이번에 바뀐 것만** 담은 짧은 블록으로 준다. 스키마 전체를 다시 돌리게 하면
    관계없는 곳까지 건드린다
  · 여러 번 실행해도 안전해야 한다 (`if not exists` · `on conflict do nothing`)
  · 표를 새로 만들었으면 `internal_all` RLS 와 권한(`grant`)도 같은 블록에 넣는다 —
    커스텀 스키마는 Supabase 기본 권한이 안 붙는다
  · 표 만들기(스키마)와 예시 채우기(seed)는 **블록을 나눈다.** 예시는 안 넣을 수도 있고,
    스키마에 섞으면 원장이 지운 줄이 다시 살아난다
